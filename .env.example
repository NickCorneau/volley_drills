# Volleycraft local development and tests do not require environment variables.
#
# Optional deploy-only values are configured outside the repo, for example as
# GitHub Actions repository secrets or Cloudflare dashboard settings.
# CLOUDFLARE_ACCOUNT_ID=
# CLOUDFLARE_API_TOKEN=
