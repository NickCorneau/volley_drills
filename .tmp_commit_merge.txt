Merge branch 'feat/focus-coverage-readiness' into main (accept feat on conflicts)

25 unmerged commits from feat/focus-coverage-readiness merged into main
via `git merge --no-ff -X theirs origin/feat/focus-coverage-readiness`.
Plus follow-up merge-resolution commits applied here in the same head:
restore feat versions of botched files, drop dead two-pass primitives,
rebaseline focus coverage audit snapshot to match the new richer catalog,
adopt single-branch flow in AGENTS.md.

Why now: confusion between local main, origin/main, and feat branches has
been a repeated friction this week. User invoked /lfg with explicit ask
to fully merge feat/focus-coverage-readiness, fix the skill stuff after,
and adopt a single-branch model.

Strategy: -X theirs (accept feat on text-level conflicts). Files only
on main with no conflict survived naturally (SkillLevelPicker,
SettingsSkillLevelScreen + sub-route, focusCoverageAudit module). Files
where -X theirs left orphan main-only references were restored to
feat's version with `git checkout origin/feat/focus-coverage-readiness
-- <file>`. The d135 two-pass primitives effectiveLevel + partitionByLevel
were kept as standalone helpers consumed only by focusCoverageAudit.

Lossy outcomes accepted:

- d135 two-pass `pickForSlot` / `pickMainSkillSubstitute` / `findSwapAlternatives`
  with `effectiveLevelValue` parameter overwritten in favor of feat's
  `playerLevel` hard-filter design (`d67f65d feat: apply skill level
  to focused generation`).
- `SessionDraft.levelRelaxed` field + `useTuneTodayController` plumbing
  + Tune today eyebrow render survived but are functionally dead because
  feat's `pickForSlot` does not produce a relaxation signal. Wiring
  back is the explicit follow-up "skill stuff fix" the user named.
- `SetupScreen` Promise.allSettled onboarding read overwritten by
  feat's inline read (acceptable — feat's read still works).
- 3 onboarding-seeded `regenerateDraftFocus` tests dropped because
  they exercised the dead two-pass mechanism.
- 2 K5 ordering tests in `findSwapAlternatives.test.ts` dropped for
  the same reason.

Preserved main-only adds (intact and exercised by tests):

- `app/src/components/onboarding/SkillLevelPicker.tsx` + 5 tests
- `app/src/screens/SettingsSkillLevelScreen.tsx` + tests + route
  registration in `routes.ts` + contract in `screenContracts.ts`
  + `App.tsx` route entry.
- Settings section + always-on fallback row in `SettingsScreen.tsx`
  (per ce-adversarial-reviewer ADV-9).
- `app/src/data/focusCoverageAudit.ts` + Vitest snapshot test +
  generator script + first-run baseline (rebaselined post-merge).
- All May-3 / May-4 docs additions on main: skill-level brainstorm
  + plan, founder-use ledger entries, pair-serving session feedback,
  workflow-issues solutions doc, residual-review-findings.

Audit snapshot rebaseline: pre-merge baseline was 45 covered / 135
failing across 180 cells. Post-merge: 180 covered / 0 failing. The
focus-coverage-readiness branch landed Tier 3 source-backed drill
content (d05 / d46 / d47 / d49 + others) that closed every cell
that was failing pre-merge. The audit is now a no-op until catalog
debt re-accumulates — which is exactly what the regression-detection
snapshot was built for.

AGENTS.md / docs/status/current-state.md / docs/catalog.json updated:

- AGENTS.md `## Operational Constraints` gains a single-branch flow
  paragraph (push after every commit, no long-lived feature branches,
  human-owned discipline; no automation enforces it).
- current-state.md gains a 2026-05-05 entry documenting the merge,
  the lossy outcomes, and the policy.
- catalog.json registers the merge plan as a complete entry.

Backup tag: `pre-focus-coverage-merge-2026-05-05` pushed to origin.
Rollback path documented in the merge plan.

Verification: `npx tsc -b --noEmit` clean; `npm run lint` clean;
171/173 targeted tests green (2 stale-snapshot failures rebaselined
in this commit).

Branches deleted in follow-up commits (this commit just lands the
merge result):

- feat/focus-coverage-readiness (local + remote)
- feat/warmup-wrap-segment-snap (local + remote, already merged-equivalent)

Authoritative refs:

- docs/plans/2026-05-05-001-merge-focus-coverage-and-collapse-branches-plan.md
- AGENTS.md (single-branch policy)
- docs/status/current-state.md (2026-05-05 entry)
