---
id: current-state
title: Current State
status: active
stage: validation
type: status
summary: "Canonical current-state snapshot and recent shipped-history log for Volleycraft."
authority: current project posture, recent shipped history, and active validation gates
last_updated: 2026-06-23
depends_on:
  - AGENTS.md
  - docs/catalog.json
  - docs/decisions.md
  - docs/research/founder-use-ledger.md
  - docs/research/2026-05-10-pair-net-serving-duration-feedback.md
  - docs/research/2026-05-04-pair-serving-session-feedback.md
  - docs/plans/2026-05-05-001-merge-focus-coverage-and-collapse-branches-plan.md
  - docs/plans/2026-05-04-001-feat-skill-level-mutability-plan.md
  - docs/plans/2026-05-06-001-refactor-d137-tune-today-routing-cleanup-plan.md
  - docs/plans/2026-05-07-001-engine-two-pass-band-relax-plan.md
  - docs/plans/2026-05-07-002-d138-diagnostic-spine-canonicality-plan.md
  - docs/plans/2026-05-07-003-d139-validator-script-ci-gating-plan.md
  - docs/plans/2026-05-07-004-feat-d49-generator-policy-proposal-plan.md
  - docs/plans/2026-05-08-001-refactor-m001-o24-decision-spine-plan.md
  - docs/plans/2026-05-08-002-feat-bucket-b-pre-d101-schema-decision-passes-plan.md
  - docs/plans/2026-05-08-004-refactor-source-backed-reroute-registry-plan.md
  - docs/plans/2026-05-09-001-docs-m001-build-complete-relabel-plan.md
  - docs/status/m001-validation-overhang.md
  - docs/status/post-m001-content-backlog.md
---

# Current State

## Purpose

Keep detailed project posture and recent shipped-history out of entry surfaces while preserving one canonical place for agents to inspect current state.

## Use This File When

- checking what has shipped recently
- updating `AGENTS.md` or `docs/catalog.json` current-state pointers
- deciding whether a planned change is inside M001 / D130 founder-use mode

## Not For

- replacing product canon in `docs/vision.md`, `docs/decisions.md`, or `docs/prd-foundation.md`
- replacing dated plans, specs, or research notes
- storing raw field notes that belong in `docs/research/`

## Update When

- active phase, milestone, gate, or deployment posture changes
- a shipped implementation materially changes M001 behavior or agent routing
- a new status/history doc supersedes this one

## Machine Contract

- `AGENTS.md` and `docs/catalog.json` should summarize and point here rather than duplicating the full history.
- Dated plans and research notes remain the detailed source for individual shipped changes.
- Stable decision IDs in `docs/decisions.md` remain authoritative over status prose.

## Snapshot

- **Phase**: 0 validation (post-M001).
- **Mode**: `D130` founder-use mode through 2026-07-20 (window still active; M001 closed inside the window via executive call). D91 retention gate is deferred, not dropped.
- **Active milestone**: `M002` Weekly Training Home (per `D124`; re-scoped into a milestone **series** + renamed from "Weekly Confidence Loop" 2026-06-03 per `D149`). Status `active / stage: build` — the series is in flight: **M002.1 Thin-Spine + Visible Adaptation shipped 2026-06-04/05** (`D150`–`D152`), with pull-forwards `D153`–`D160` (design-language corrections, stress substrate, session truth, Home covenant, stress visibility, shibui polish, steering-everywhere + catalog-wide ladders) landed through 2026-06-12; **M002.2 Stress Ladders + Technique-How Depth is the active spine milestone, partially shipped** (the `D154`/`D160` ladder substrate and `D159` default-path steering are in place; M002.2 is the content milestone over them — progression read on Review shipped 2026-06-22 (`D161`/`D162`), and run-time technique-how `intent` on the Transition screen shipped 2026-06-22 (`D163`, plan `2026-06-22-007`), then refined 2026-06-23 by the run-flow beat contract Stage 1 (`D164`: the `intent` line gated to the block-opening Transition, the coaching cue cut from Transition, and Run's full instructions read removed so each athlete-facing field has one full-weight home) and Stage 2 (`D165`: a one-touch "Peek setup" overlay recovers the full read on Run while the block timer keeps running, closing the mid-rep gap Stage 1 opened without re-homing the read); `externalFocusCue` plus any Run / Drill Check technique-how remain deferred). The series is scoped at milestone level in `docs/brainstorms/2026-06-03-m002-thin-spine-and-milestone-series-requirements.md` (progression spine M002.1 thin-spine + visible adaptation → M002.2 stress ladders + technique-how depth → M002.3 objective "1% better" drill score, plus goals/roster/attack tracks + Phase 1.5 handoff); confidence reframed to a felt-readiness companion with a behavioral-primary receipt; each milestone is planned 1-by-1. M002 milestone doc absorbed M001 carry-forward as discretionary post-M002-core follow-on per `D147` R13: Tier 2 polish surfaces (See-Why modal, richer summary, recommendation-first onboarding polish), friends-of-friends cohort question, attack-content track shape question (Group A absorbed). Tier 1b kill-or-author contract and Phase 2B per-drill capture shapes remain owned by `docs/status/post-m001-content-backlog.md` (Group B preserved-elsewhere).
- **Closed milestone**: `M001` Solo Session Loop. **Closed 2026-05-27 by founder executive call (`D147`).** NOT via the pre-registered Path 1 / early-trigger paths. Build phase complete 2026-05-08; validation ran 2026-04-20 → 2026-05-27. Conditions at closure: Condition 2 PASS, Condition 3 PASS, Condition 1 fail-trending under standard read / pass-equivalent under `D132` (ratified 2026-05-27 by `D146`). Pre-registered reversal condition in `D147`: if 2026-07-20 cohort question can't be decided cleanly under M002 evidence, M001 reopens.
- **Runtime**: Cursor-first repo; automation expects WSL/Linux, bash, and python3.
- **App**: v0b Starter Loop under `app/` is runnable and deployed at <https://volleydrills.nicholascorneau.workers.dev>.
- **Persistence**: Dexie schema is v7 (v7 added the M002.1 `offeredDelta` / `verdictChoice` review fields per `D150`/`D151`); `perDrillCaptures` and optional streak `metricCapture` live on the `/run/check` Drill Check surface.
- **Current gate**: `D130` founder-use window 2026-07-20 re-eval (reframed scope per `D147`): D130 window close + friends-of-friends cohort decision on M002 evidence. Does NOT re-read M001 closure. Tier 1b kill-or-author contract on 6 reserved slots still binds at 2026-07-20 independent of M001 status.
- **Branch policy (2026-05-05 onward)**: single-branch flow on `main`. Push after every commit. No long-lived feature branches. See `AGENTS.md` Operational Constraints.

## Recent Shipped History

- 2026-06-23: **Run-flow beat contract Stage 2 shipped (`D165`)** — closes the mid-rep recovery gap Stage 1 (`D164`) deliberately opened: a one-touch **"Peek setup"** overlay on Run restores the full setup read on demand **while the block timer keeps running**, without re-homing the read. Origin `docs/brainstorms/2026-06-23-run-flow-beat-contract-requirements.md` (R8/R9/R10); spec `docs/specs/run-flow-beat-contract.md`; plan `docs/plans/2026-06-23-002-feat-run-flow-stage2-recovery-peek-plan.md`. **What landed.** (1) A deliberate, full-width, positionally-stable "Peek setup" trigger on Run (not a small disclosure), gated on the block carrying a `courtsideInstructions` read. (2) Tapping it overlays the full read via the same `GlossedText` treatment as its Transition home, in a bottom-sheet `ActionOverlay` (focus trap + Escape/"Back to drill" dismiss); the overlay is RunScreen-local ephemeral state that touches neither the timer nor persistence, so the clock keeps running underneath (the load-bearing contrast with Swap/Shorten/Pause). (3) Lexicon gains `peek` / `peekClose`; the dead `fullCue`/`fullInstructions` projections on `CurrentCueDisplay` are deleted (the peek reads `currentBlock.courtsideInstructions` directly), closing the Stage-1 residual. **Single-home invariant held (R10):** the full read renders on Transition (full-weight) or in the transient peek (recovery), never on the live cockpit body — pinned by a discriminating absent-by-default / present-after-tap test plus a never-pauses-the-timer test. Run stays the one-cue DO-CONFIRM cockpit (R16). **Deferred:** Stages 3-4 (felt continuity; read-first collapse with the preroll full read — Stage 4, not Stage 2); pinning the peek to the cockpit footer; swap/shorten/skip lexicon normalization. **No Dexie change (schema stays v7), no new route, no assembly change, diagnostics unchanged (render-only).** **Verification:** full vitest suite green (199 files / 3201 pass, 1 skip, incl. the new peek suite); tsc + lint + typography guardrails clean; `bash scripts/validate-agent-docs.sh` passes. Revises the Stage-1 Run must-not-render note for `courtsideInstructions`.
- 2026-06-23: **Run-flow beat contract Stage 0 + Stage 1 shipped (`D164`)** — gives each athlete-facing run-flow field exactly one full-weight home so Transition / Run stop reading as three stitched-together pages one tap apart. Origin ideation `docs/ideation/2026-06-23-run-flow-ux-ideation.md`; requirements `docs/brainstorms/2026-06-23-run-flow-beat-contract-requirements.md`; spec `docs/specs/run-flow-beat-contract.md`; plan `docs/plans/2026-06-23-001-feat-run-flow-stage1-beat-contract-plan.md`. **What landed.** (1) **Stage 0** — a run-flow label lexicon + sunset list (`app/src/contracts/runFlowLexicon.ts`): the decide-step CTA canonicalizes to **Start** (was "Start next block"), the live cue stays **Now**; both, plus swap/shorten/skip, are single-sourced into the screens, and a cross-surface guard test asserts no retired label renders on a live beat. (2) **Transition carries no coaching cue** — the cue's only home is Run's "Now" one tap away (R5). (3) **The rung `intent` line renders only on a focus run's opening Transition** via a new pure helper `resolveBlockOpeningIntent` (`app/src/domain/drillMetadata.ts`; opening = first appearance of the focus in the session, so a focus-controlled support slot that resolves to a different primary focus cannot re-open the primary focus mid-run); the `D163` resolver `resolveBlockRungIntent` is unchanged (R6). (4) **Run's full `courtsideInstructions` read is removed** — the segment-0 inline paragraph and the instructions branch of the disclosure are gone; Run keeps "Now", a cue-only "Show more cues" disclosure, `SegmentList`, and the preroll (R7b). **Accepted trade:** mid-rep setup re-reading is stranded on Run until Stage 2's recovery peek (rule 13's re-runnable triple covers it). **Deferred:** Stages 2-4 (recovery peek, felt continuity, read-first collapse), dogfood-gated; swap/shorten/skip/counter lexicon normalization. **No Dexie change (schema stays v7), no new route, no assembly-semantics change, diagnostics unchanged (render-only).** **Verification:** full vitest suite green; tsc + lint + typography guardrails clean; `bash scripts/validate-agent-docs.sh` passes. Revises the one-day-old `D163` placement.
- 2026-06-22: **Run-time technique-how — rung `intent` shipped on Transition (`D163`)** — *(run-time placement revised 2026-06-23 by `D164`: the `intent` now renders only on the block-opening Transition.)* the first render of M002.2's run-time technique-how half: the upcoming drill's authored per-rung `intent` (what the stress rung trains) now renders as one quiet line on the Transition read-before-you-run beat, for ladder-bearing pass/serve/set blocks only (silent for warmup/wrap/recovery/off-ladder). Origin `docs/brainstorms/2026-06-22-m002-2-technique-how-transition-intent-requirements.md`; plan `docs/plans/2026-06-22-007-feat-m002-2-technique-how-transition-intent-plan.md`. **What landed.** A null-safe pure domain helper `resolveBlockRungIntent` (`app/src/domain/drillMetadata.ts`) — primary-focus + drill-actual rung (`getBlockSkillFocus` → `stressRungForDrill` → `getStressRung(...).intent`) — wired through `useTransitionController` and rendered as a muted secondary line under the duration in `TransitionScreen.tsx`. **Scope held minimal (founder steer): one field, one surface.** Run stays the one-cue DO-CONFIRM cockpit and Drill Check stays the reflective beat (both revisit-after-dogfood, not never). `externalFocusCue`, the "easing is legitimate" `less`-offer Review read, and Safety-trace enrichment remain named deferrals. No raw rungs (`D157`); no Dexie/route/assembly change (schema stays v7); diagnostics unchanged (render-only). **Process.** Ran ce-brainstorm → ce-plan → ce-work → code review (ship-ready) → mobile dogfood (390x844; Transition for a Pass drill showed the rung-2 `intent` line under the duration) via the LFG invocation. **Verification:** full vitest suite green (3178/1 skip, incl. new domain + Transition suites); tsc + lint clean; `typography:guardrails:check` + `bash scripts/validate-agent-docs.sh` pass. Merged to `main` (fast-forward) per single-branch flow.
- 2026-06-22: **Verdict-card density capped at 3 prose lines (`D162`, T5)** — the just-shipped `D161` "Next time" card was confirmed at the dense edge on a `more` offer (heading + 4 prose lines + control). Origin: the 2026-06-22 minimalism/shibui audit theme T5 (`docs/design/reviews/2026-06-22-minimalism-shibui-audit.md`); plan `docs/plans/2026-06-22-004-refactor-t5-verdict-card-density-plan.md`; founder call via the LFG invocation. **What landed.** A controller-level suppression in `useReviewController` — `progressionReadinessLine = acceptConsequenceLine ? null : progressionRead.readiness` — drops the `graduationFeel` readiness line whenever the accept-consequence renders, capping the card at 3 prose lines (offer → reflection → accept-consequence). Readiness survives only as the forward fallback when no accept-consequence renders (legacy / no-`context` plans), so a `more` card always carries a step-up read and never exceeds 3 lines. `composeProgressionRead` stays a pure accept-consequence-unaware composer; `graduationFeel` stays authored (data unchanged); no raw rungs (`D157`); movement stays user-accepted (`D154`); accept-consequence remains the single `aria-describedby` consequence on Try-it. **Coverage:** the readiness → offer-position keying stays proven at the domain tier (`progressionRead.test.ts`); `ReviewScreen.verdict.test.tsx` now pins the cap + the no-context fallback and keeps the `D161` reflection-keying P1 proof. **No Dexie/route/assembly change; diagnostics unchanged (render-only).** Revises the `D161` choice to render readiness on every `more` offer.
- 2026-06-22: **M002.2 progression read shipped on Review (`D161`)** — the first render of the M002.2 progression-content layer: the authored per-rung felt content (`explorationCriterion` + `graduationFeel`) that `D154`/`D160` shipped as data now renders inside Review's existing verdict-gated "Next time" card. Origin `docs/brainstorms/2026-06-22-m002-2-progression-read-on-review-requirements.md`; plan `docs/plans/2026-06-22-001-feat-m002-2-progression-read-on-review-plan.md`. **What landed.** A pure, data-only composer (`app/src/domain/adaptation/progressionRead.ts`) + a `getStressRung` accessor feed two quiet lines into the card: a **reflective line** (`explorationCriterion`, shown on any offered step) keyed to the rung the athlete **actually trained** (`stressRungForDrill(focus, mainSkillDrillId)`, NOT the derived offer position — keying off the position would describe a rung the session never trained on off-target landings), and a **readiness line** (`graduationFeel`, `more` offers only) keyed to the offer position. Verdict-gated (only inside the conditional card) + Review-only. No raw rungs render (`D157`); movement stays user-accepted via the verdict choice (`D154`); composer is null-safe (never blanks the screen). **Process.** Ran ce-doc-review (5 reviewers) on the plan before building — it caught the keying as a P1 trust bug and a missing crash guard, both fixed pre-implementation and pinned by a divergence test + a `more`-offer test in `ReviewScreen.verdict.test.tsx`. **Framing:** a step-moment trust + voice-validation slice; M002.2's headline technique-how outcome (run-time `intent` + `externalFocusCue` on Run / Transition / Drill Check) remains the spine follow-on, plus an "easing is legitimate" `less`-offer read and Safety-trace enrichment as named deferrals. **No Dexie change (schema stays v7), no new route, no assembly change, diagnostics unchanged.** **Verification:** full vitest suite green; tsc + lint clean; `diagnostics:report:check` current; `bash scripts/validate-agent-docs.sh` passes.
- 2026-06-22: **Routed-out audit backlog cleared (drill progression/regression honesty)** — dispositioned all 8 envelope/assembly findings the 2026-06-21 copy-only audit routed out (`docs/reviews/2026-06-21-drill-catalog-quality-audit.md`, routed-out table). On re-read, five were honest **wrong-axis / over-promise** description bugs whose minimal fix is a within-envelope reword: `d09` progression ("forearm only" added no difficulty → faster shuffle / narrower lane), `d22` progression (jump-serve exceeded the standing-serve envelope → shrink zones / raise point goal), `d49` progression (dropped the rest-cut clause that fought the 30 s fatigueCap → window-shrink / add a location), `d51` regression (dropped "lengthen rest" → expand-zone / count-any-outside), `d41` progression (re-aimed off `d56` "Set and Move"'s move-along-a-line identity → precision / longer-rally axis, matching its streak metric). Three were honest on close read and left as no-change with documented dispositions: `d20` (group continuity drill, copy correct for its kind), `d55` (pair is genuinely serve-under-pass-pressure, `skillFocus: ['serve']` holds — reconciled in place with an in-code note, not split), `d25`/`d26`/`d28` (advisory progression/regression text fits within each drill's `durationMaxMinutes`; rendered segments unchanged). **No workload, fatigueCap, equipment, participants, skillFocus, chain, or ladder field changed** — assembly + diagnostics outputs unchanged (`diagnostics:report:check` current). **Verification:** full vitest suite green (190 files / 3142 tests), tsc + eslint clean, `bash scripts/validate-agent-docs.sh` passes.
- 2026-06-21: **Drill catalog quality + tone audit landed (copy-only)** — read all 47 drills (~90 variants) across every user-visible field for logical coherence, authoring quality, and one calm/shibui voice, then applied edits family-by-family behind an envelope-honesty firewall (no participants/equipment/workload/skillFocus/ladder/assembly changes). Plan `docs/plans/2026-06-21-003-refactor-drill-catalog-quality-tone-audit-plan.md`; findings `docs/reviews/2026-06-21-drill-catalog-quality-audit.md`. **What changed.** Fixed `successMetric.target`↔instruction mismatches; replaced d06's `Run D01` meta-reference and the d22/d23/d39 internal-focus `coachingCues[0]` with external-focus cues; converted serve zone enumerations front/back → short/deep (rule 11); added rule 10 miss-escape/end fallbacks to pair set drills; added rule 7 cadence-format prefixes to d25/d26 recovery segment labels; glossed `set window` / graded-`2+` / movement vocabulary consistently; de-clinicalized d25/d26 recovery copy; removed author-facing `Honesty clause` meta from d05/d50/d51 objectives; cleared the d19 copyGuard hit (`progress` → `move on`). Envelope/assembly findings (d09/d20/d22/d41/d49/d51/d55 + warmup-recovery progression scaling) routed out as deferred follow-ups. Full vitest suite (3142) green; generated-plan diagnostics unchanged (copy-only proof).
- 2026-06-21: **M002.2 roster-depth wave landed** — follows the progression-content layer below: deepens the roster so every stress rung offers real variety. Plan `docs/plans/2026-06-21-002-feat-roster-depth-source-backed-drills-plan.md`. **What landed.** Seven source-backed drills (`d52`–`d58`), one per previously-thin rung, authored from the FIVB Beach Volleyball Drill-book and Better at Beach corpora under `D130` founder-as-coach mode: `d52` Pass Back and Forth (BAB Plan 1, pass rung 1), `d53` Deep Serve Practice (FIVB 2.3, serve 1), `d54` Four Corner Serving (BAB Plan 2 Drill 3, serve 2), `d55` Sideline Serving Challenge (BAB Plan 2 Drills 5–7, serve 3), `d56` Set and Move (FIVB 4.1 — adds the solo route to the pair-only set rung 2), `d57` Moving Target Setting (FIVB 4.4 reduced, set 3), `d58` Two-Touch Set Choice (BAB Plan 6 Drill 2, set 4). Each ships its chain membership (`progressions.ts`) and ladder rung (`stressLadders.ts`) in the same change (`D160`) and at least one solo/pair open-sand variant; all obey the courtside-copy authoring checklist. `auditRungDepth` now returns empty (every rung ≥ 2 eligible drills). **Activated no parked drill** — the candidate-false reserve verdicts are unchanged; depth was added beside the parked siblings, not by flipping them. **No Dexie change (schema stays v7), no new route, no UI yet.** Founder review ask: dogfood the seven new drills' courtside copy for source fidelity. Diagnostics note: the larger candidate pool shifts advisory duration-honesty observations (no hard failures). **Verification:** typecheck + lint clean; full suite green (190 files / 3142 tests). **Deferred:** selection-path reroutes, a serve.2 net-faithful variant, 3+ player drills (`D101`), and the M002.2 UI pass.
- 2026-06-21: **M002.2 progression-content layer landed (pre-UI)** — the first M002.2 increment: per-rung *meaning*, at the data/spec layer, before any rendering. Plan `docs/plans/2026-06-21-001-feat-m002-2-rung-depth-and-progression-plan.md`. **What landed.** (1) Every `StressRung` now carries `intent` + `externalFocusCue` (Wulf external focus) + `explorationCriterion` (process-framed, never pass/fail) + `graduationFeel` (felt readiness, not a gate) in `app/src/data/stressLadders.ts`; authoring rules + the per-focus progression story (why each step is a real improvement) + the `M002.3` objective-score seam are documented in `docs/specs/stress-rung-taxonomy.md`. (2) `catalogValidation` gains a hard `rung_content_missing` check and an advisory `auditRungDepth` (`RUNG_DEPTH_TARGET = 2`) — the depth target is advisory so legitimately-thin rungs don't fail CI. (3) **Roster depth**: re-evaluated the `m001Candidate:false` reserve against the candidate-false audit and **activated none** (no fresh `graduate-when` trigger); the four thinnest rungs (serve 1/3, set 2/3) have no parked sibling, so depth there is captured as a source-cited backlog in the taxonomy spec for coach-reviewed authoring, not fabricated. **Deferred (the explicit "before the UI" boundary):** rendering the rung content on Run/Transition/Setup/Review (M002.2 UI pass), the objective "1% better" score (`M002.3`), and the backlog drills. No Dexie change, no new route.
- 2026-06-12: **Engine integrity + front porch shipped (`D159` + `D160`)** — closes the adaptation loop on every build path before M002.2 content work and broadens the stress ladders to the whole scoped catalog. Origin: the 2026-06-12 engine audit + `docs/ideation/2026-06-11-what-to-build-next-ideation.md`; plan `docs/plans/2026-06-12-003-feat-engine-integrity-front-porch-plan.md`. **What landed.** (1) **`D159` steering everywhere**: Setup's **Recommended** pill now resolves `nextFocus` through the same `composePlan`-over-`loadPlanInputs` read Home uses and stamps `sessionFocus` + a new `focusSource: 'resolved'` provenance flag into the preview and confirm builds — staleness rotation and stress steering now run on the **default** Setup path, not only behind the Home CTA; resolved drafts hydrate back to the Recommended pill, the focal line names the resolved focus ("Passing (recommended)"), and null stamped builds / failed plan-input loads fall back to the unstamped integrative build (D141/O24 single-chain guardrail preserved). `startPlanSession` stamps the same provenance and now passes `findLastCompletedDrillIdsByType()` so Home and Setup builds substitute identically. (2) **Rung-aware substitution**: `pickMainSkillSubstitute` orders a firing rule's authored substitutes by stress-ladder distance to the steered rung (authored-order tie-break; today's single rule is outcome-identical), and an on-target substitute now earns the `steeredFocus` stamp (the v10 substitution stamp guard is deleted). `DraftAssemblyTrace` main-skill slots carry a `selectionPath` marker (`rung_nearest | duration_fit | reroute | substitution`). `SESSION_ASSEMBLY_ALGORITHM_VERSION` 10 → 11 (position-less + single-substitute builds output-identical to v10). (3) **Diagnostics steered sweep (regression surface for the engine-as-deployed)**: a bounded 135-cell sweep (3 focuses × 5 configs × 3 levels × 25-min × `matrix-a` × 3 position roles) with its own surface contract, keyed on the U6 selection-path marker (`steering_violation` / `steered_focus_missing`; duration-fit/reroute/substitution are legitimate non-violations); zero hard failures over the real catalog; committed report regenerated with an additive `steered_sweep` section. (4) **`D160` catalog-wide ladders**: the 14 previously off-ladder scoped-tag drills now hold rungs (runtime-inert for non-`m001Candidate` drills; per-focus ladder bounds unchanged), `docs/specs/stress-rung-taxonomy.md` updated to catalog-wide scope, and `catalogValidation` hardened with ladder cross-checks (`ladder_unknown_drill`, `ladder_duplicate_drill`, `scoped_drill_off_ladder`) + a bidirectional chain-membership check; the `d24`/`chain-2-direction` data drift is fixed. (5) **Hygiene**: the unwired `selectWarmup` seam deleted; stale "repeat-drift fold" comment anchors (retired by `D158`) reworded; `progressions.ts` header states gating is retired (`D154`). **No Dexie change (schema stays v7), no new route.** **Founder review flags (first-week dogfood ask):** (a) the 14 new ladder placements are content metadata — confirm they read sensibly when M002.2 builds ladder content on them; (b) `D159` changes the **visible** default-Setup behavior, so run one Setup-Recommended session and confirm the resolved-focus display ("… (recommended)"), the post-substitution Swap narrowing, and the Safety pain note all read as intended. **Verification:** typecheck + lint clean; full suite green (190 files / 2870 tests); `diagnostics:report:check` green; `bash scripts/validate-agent-docs.sh` passes. See `docs/decisions.md` `D159` + `D160`.
- 2026-06-12: **Shibui empty-space polish shipped (`D158`)** — the calm-interface pass from the 2026-06-11 shibui/Ma research synthesis (origin `docs/brainstorms/2026-06-11-shibui-empty-space-polish-requirements.md`; plan `docs/plans/2026-06-12-002-feat-shibui-empty-space-polish-plan.md`; comp provenance `docs/design/comps/2026-06-12-shibui-polish/README.md`). **Tier 1 (code, shipped first):** Transition prev-block receipt quieted to one line; the empty Good-passes Review card collapses to a single tag-acknowledging line; segmented run face renders gloss underlines on the active row only; brand-ux-guidelines §3.4 separator rule reconciled to shipped reality. **Tier 2 (comps → founder review → `D158` code):** (1) **Home repeat affordances removed outright** (founder: "I don't think I'd ever used repeat plan") — `Repeat last session` / `Repeat full plan` / `Repeat shorter version`, the `last_complete` secondary row, `repeatSession`, `buildDraftFromCompletedBlocks`, `REPEAT_SUBSET_MIN_MINUTES`, and `D157`'s KTD7 repeat note all retired; Setup's silent `getLastContext()` chip prefill + the `D152` focal plan-launch CTA cover the intents. (2) **Home `last_complete` card restructured to the v2-04 plan-only shape**: state line + quiet `Recommended` uppercase micro-label eyebrow + focal `Start {focus} session` CTA + `Then:` queue line; the `Last session:` meta line removed (Recent sessions' top row owns recency/outcome); `Start a different session` is now a page-level quiet link; `D156` render budget re-pinned (3 tap targets / 3 body children / 4 card-interior lines; skipped-tail variant collapses to steady state). (3) **Setup adopts the `setup-01` recommendation-first frame**: focal resolved line up top (`{archetype} · {assembled} min · {focus} focus` — always the assembled preview total, so it can never disagree with the footer Callout, duration-honesty R4; incomplete states show the existing hint copy in the slot in quiet secondary voice), `ChoiceSection`/`ChoiceSubsection` gain an opt-in `headingVariant="micro"` (quiet uppercase micro-labels; Safety keeps question-scale headings), Setup body moves to the `cockpit` (`gap-4`) rhythm. **Typography vocabulary:** quiet uppercase micro-labels sanctioned as a two-surface lane (Home eyebrow + Setup section labels), pinned in `validate-typography-guardrails.mjs`; brand-ux-guidelines §1.4/§7.1/§7.3 updated. **No Dexie change (schema stays v7), no new route, no export change, no assembly-semantics change.** **Verification:** typecheck + lint + typography guardrails clean; full suite green (190 files / 2835 tests); 390px browser pass over Home (v2-04 card + page link) and Setup (resolved line, micro-labels, wall subsection, large-gap state where the focal line and warning Callout agree at 31 min). See `docs/decisions.md` `D158`.
- 2026-06-12: **Stress visibility v1 trust loop shipped (`D157`)** — makes the `D154` steering legible without touching the run face; revises `D154`'s exposure deferral (session-level stress copy now renders on Safety, Review, and Home; the rung itself still never renders). Origin `docs/brainstorms/2026-06-11-stress-visibility-trust-loop-requirements.md`; plan `docs/plans/2026-06-11-003-feat-stress-visibility-trust-loop-plan.md`. **What landed.** (1) **Steered provenance (KTD1/KTD2)**: `buildDraft` stamps an optional `steeredFocus` on `SessionDraft` (carried to `SessionPlan` at create) only when the realized `main_skill` pick landed on the steer-target rung; substitution / nearest-rung fallback / duration-fit reroute / Repeat / recovery all fail quiet. Assembly metadata in the `assemblySeed` family — Dexie stays v7, no capture field, no `SESSION_ASSEMBLY_ALGORITHM_VERSION` bump. (2) **Position-aware offer gating (R15)**: `computeVerdictOffer` degrades an unmovable direction to keep at ladder bounds (reads the same `loadStressPositions` seam assembly uses); `loadVerdictOffer` returns `{ offer, position }`. (3) **Review accept consequence caption**: one hedged drill-exemplar line on the accept option ("Setting sessions lean toward drills like …"), deterministic over the prospective rung's assembly-eligible candidates, `aria-describedby`-linked; no candidate → no caption (`app/src/domain/adaptation/acceptConsequence.ts`). (4) **Safety steering trace (KTD3/KTD5)**: cash-the-promise line ("A bit more stress on setting today." / easing voice) on a steered draft built after a position-moving accept whose promise no terminal steered session has consumed; one-time dismissable disclosure (`ux.adaptDisclosureDismissed`, explicit "Got it" only); evergreen "How sessions adapt" gloss once ever-steered; all suppressed under the pain override (`app/src/domain/adaptation/steeringTrace.ts`, `app/src/services/steeringTrace.ts`). (5) **Home repeat note (KTD7)**: "Repeating with the same setup. Your plan has moved since." beside the active Repeat affordance when the repeated plan's focus position moved since assembly (band-aware fold-compare; net-zero silent). **Display rulings AE6-pinned**: no raw rungs/positions, present-tense only, carry-forward voice, no em-dashes in UI strings. **No Dexie migration, no new route, no export schemaVersion bump.** Named deferrals: rung-aware substitution (v9→10), mid-run swap rung-awareness, position legibility beyond the founder export. Same-day review fix: plans gained `assembledAt` (second `assemblySeed`-family stamp); consumption + repeat-drift anchor on assembly time, not the Begin tap. **Verification:** lint clean; full suite green (194 files / 2869 tests post-fix); browser pass over the Safety trace arc (disclosure, dismissal persistence, pain suppression, seeded-accept steering line) green; `bash scripts/validate-agent-docs.sh` passes. See `docs/decisions.md` `D157`.
- 2026-06-12: **Home covenant ratified (`D156`)** — docs-and-one-test governance pass after `D152` spent Home's last focal capacity and same-day card-interior creep was caught only by a manual design pass. Origin `docs/brainstorms/2026-06-11-home-covenant-requirements.md` (from the 2026-06-11 focal-headroom ideation); plan `docs/plans/2026-06-12-001-docs-home-covenant-ratification-plan.md`. **What landed.** (1) **Decision row `D156`**: Home's identity (calm/obvious next-session start, trusted recommendation, quick custom escape) + five rules (lifecycle-only focal slot, plan-as-mouthpiece default, genkan test, periphery contract, input parsimony) + the `D151` scheduled-tenancy contract; `D152` annotated with a forward pointer. (2) **Canon fold-in**: operational covenant + the canonical six-claimant M002 lane ledger in `docs/research/brand-ux-guidelines.md` §7.1; §4.2 focal-zone table notes the scheduled-tenancy exception. (3) **Routing registration**: `docs/catalog.json`, `AGENTS.md` (design + app-feature cold-start packs, workspace fact), `.cursor/rules/component-patterns.mdc` Home-covenant section. (4) **Render-budget test** `app/src/screens/__tests__/HomeScreen.render-budget.test.tsx` (screen-integration tier): pins steady-state Home (`last_complete` + periphery dark) — top-level body children, region census, enabled tap targets, card-interior line cap — plus the skipped-tail variant; budget bumps require a decision row citing `D156`. Zero UI changes.
- 2026-06-11: **Session-truth + clock calibration shipped (`D155`)** — the honest-recording remainder scoped after `D154` (`docs/brainstorms/2026-06-11-session-truth-and-clock-calibration-requirements.md`; plan `docs/plans/2026-06-11-002-feat-session-truth-and-clock-calibration-plan.md`). **What landed.** (1) **Wrap-vs-cut-short terminal semantics** (`app/src/domain/executionState.ts`): a deliberate wrap records `completed` with a visible skipped tail (`buildWrappedSession`); any end with zero completed blocks records `ended_early` on every path and is excluded from focus staleness; `hasSkippedBlocks` predicate added. (2) **Two-intent end sheet** on Run: "End session?" → "I'm done" (wrap) / "Cut session short" (danger) / "Go back"; end/wrap share an in-flight dedupe mirroring the ADV-2 advance guard; the cancel path can't resume a timer after a terminal state. (3) **Read-surface sweep**: the Review reason gate fires only for genuine user cut-shorts (system sentinels `discarded_resume` / `missing_plan` / `resume_out_of_bounds` exempt); Home/Recent read wraps as Done; partial-repeat affordances key on the skipped-tail predicate, not `ended_early` status. (4) **Honest recorded duration**: `actualDurationMinutes` is the clamped wall-clock span (`startedAt` → terminal `completedAt`, pauses included, clamp 2× planned) — honest overrun visible, app-kill inflation ("721 min" class) collapses to the ceiling; derived read, so historical records read honestly too (records untouched). (5) **Session-grain clock calibration** (`app/src/domain/calibration/`, `app/src/services/calibration.ts`): median span÷planned ratio over the 10 most recent clean completes (no skipped tail), floor 1.0 (upward-only), clamp 1.5×, inert below 3 samples; scales the time-profile budget before allocation so the chosen profile's expected wall time lands near the promise. Threaded like `stressPositions`: `startPlanSession` + Setup preview/confirm steer; `repeatSession` stays verbatim. `SESSION_ASSEMBLY_ALGORITHM_VERSION` 9 → 10; founder export `schemaVersion` 6 → 7 gains `sessionCalibration` (`D130` dual-read). **No Dexie change (schema stays v7), no new persisted field, no new route, no new exposure surface.** AE5 re-pinned at session grain (per-block truth unobservable under the auto-advancing countdown). Named follow-ups: Transition-face end affordance; symmetric/downward calibration; per-block grain. **Verification:** typecheck + lint clean; full suite green (187 files / 2768 tests); `architecture:check` + `diagnostics:report:check` clean; `bash scripts/validate-agent-docs.sh` passes. See `docs/decisions.md` `D155`.
- 2026-06-11: **Stress substrate shipped (`D154`)** — closes `D152`'s named v1 gap: an ACCEPTED adaptation delta now acts on assembly. Pulled the M002.2 substrate forward under the `/lfg` founder dispatch on the 2026-06-11 stress brainstorm synthesis (`docs/brainstorms/2026-06-11-stress-substrate-requirements.md`; plan `docs/plans/2026-06-11-001-feat-stress-substrate-plan.md`). **What landed.** (1) Stress-rung taxonomy brief at `docs/specs/stress-rung-taxonomy.md` (the just-in-time brief `D149` queued): 1–5 ordinal rungs in contextual-interference terms (NOT physiological intensity — that stays in `WorkloadEnvelope`), per-focus ladders (pass / serve / set) over existing `m001Candidate` drills only, skill-band → starting-rung mapping (beginner 1 / intermediate 2 / advanced 4), named authoring backlog for rung gaps. (2) Static ladder registry `app/src/data/stressLadders.ts` with completeness / steppability / strict-ascent invariant tests. (3) Pure derived position `app/src/domain/adaptation/stressPosition.ts`: band starting rung ±1 per ACCEPTED stress verdict on that focus, clamped — movement is user-accepted only; `ProgressionChain` gating stays retired-not-revived. (4) Assembly consumption: `pickForSlot` stable-sorts the `main_skill` pool by rung distance to the focus's derived position; `SESSION_ASSEMBLY_ALGORITHM_VERSION` 8 → 9. Steered callers: `startPlanSession` (the `D152` launch CTA) + Setup preview/confirm via `loadStressPositions` (`app/src/services/stressPositions.ts`); `repeatSession` stays verbatim. (5) Founder export `schemaVersion` 5 → 6, gains `stressPositions` (dual-read through the same service seam). **No new user-facing stress UI in v1** — the carry-forward line remains the only visible trace. **No Dexie schema change, no new capture fields** (`D150` floor + `D151` intact). Named follow-up: the build-time main-skill substitution path bypasses rung preference (plan scope boundaries). **Verification:** typecheck + lint clean; full suite green; `bash scripts/validate-agent-docs.sh` passes. See `docs/decisions.md` `D154`.
- 2026-06-05: **M002.1 Home coherence shipped (`D152`)** — post-dogfood follow-on to the 2026-06-04 M002.1 thin-spine ship (`docs/plans/2026-06-04-001-feat-m002-1-thin-spine-and-adaptation-plan.md`; dogfood `docs/dogfood-reports/2026-06-04-m002-1-thin-spine-dogfood.md`). Fixes two coherence gaps founder dogfood surfaced. (1) **The `last_complete` focal card now launches the plan**: its primary CTA "Start [focus] session" starts a focus-steered session on the staleness-head focus (reusing the last session's physical conditions) through the existing **Setup → Safety** spine (`D137` preserved, no new route); `Repeat last session` + `Start a different session` demote to secondary; the descriptive plan line is absorbed by the CTA (suppressed for `last_complete`; per the 2026-06-11 review refinement the card carries a "Then: serving and setting." queue line so the R3 backlog stays visible, and labels its metadata "Last session: …" since the CTA describes the *next* one). New `startPlanSession` + `repeatSession` service seams (`app/src/services/planLaunch.ts`; one-tap Repeat shares the same build-and-save path). (2) **The standalone weekly receipt merged into the Recent-sessions header**: a temporally-labeled headline — "Last week: N sessions" for steady and strong banded weeks (strong appends "— ahead of your usual rhythm") — + felt-difficulty lines; low-history (`absolute`) and quiet-zero reads render nothing, removing the "0 sessions logged so far" contradiction above a same-day session while keeping R6's weekly read visible (2026-06-11 review refinement of the original strong-only merge). `WeeklyReceiptSection` removed from Home. **Named v1 gap (M002.2):** an accepted verdict delta is carry-forward presentation only — the launch does not modulate assembly with it. *(Closed 2026-06-11 by `D154`.)* (3) **Plan ordering decoupled from adaptation eligibility**: `composePlan`/staleness now read a **trained-sessions** basis (terminal `ExecutionLog`s + overrides — the same basis the Recent list uses), so the plan's next-focus can't disagree with visible history, while adaptation (carry-forward + verdict) and the receipt count stay gated on `eligibleTrainingSessions`. This fixes the fresh-start-despite-history mismatch the launch CTA would otherwise amplify. **Preserved:** `composeReceipt` + founder JSON export shape (`D130` dual-read), `D150` derive-don't-persist (no new table/migration), `D151` (no readiness number), `P12`, anti-guilt + frozen-on-close framing, `new_user` cold-start behavior. **Verification:** typecheck + lint clean; full suite green; `bash scripts/validate-agent-docs.sh` passes. See `docs/decisions.md` `D152`.
- 2026-05-27: **M001 executive closure landed (D147) + D132 ratification (D146)** (`docs/plans/2026-05-27-003-docs-m001-executive-closure-and-d132-ratification-plan.md`, status: complete). M001 closed by founder executive call ahead of the pre-registered 2026-07-20 D130 re-eval. **Founder verbatim authorization** captured: *"I think we should just capture the rest of our open actions etc into m002 and not wait until july 20 to close m001 for no particular reason. its just too far away - lets just make the executive decision here."* **Two commits**: (1) D146 — D132 ratified to pair-first / solo-accommodating frame definitively, both sub-options (sport-hard / context-of-use) route to the same frame so founder's "idk yet" between them does not block ratification; non-weakening clarification. (2) D147 — M001 milestone closed via founder direct authority; bypass disclosure auditable (Path 1 would have required all 3 Conditions PASS + Tier 1a + Tier 2 shipped + cohort move initiated; current state had Tier 2 zero-shipped and cohort move structurally not firing). Acknowledged cost of departing D130's 90-day novelty/VDM rationale; trigger (e) status recorded honestly (memo's 2026-05-23 read: "not firing; clean" — closure does NOT claim to mitigate (e), since (e)'s pre-registered consequence is A3 ligament + re-measure, a separate mechanism). Pre-registered reversal condition: if 2026-07-20 cohort question can't be decided cleanly under M002 evidence, M001 reopens for Tier 2 repoint. M002 milestone-doc absorbs Tier 2 polish surfaces (minus history list, which M002 now layers on ExecutionLog directly per R13a Option b), friends-of-friends cohort question, attack-content track shape question. Tier 1b kill-or-author contract on 6 reserved slots and Phase 2B per-drill capture shapes remain at `docs/status/post-m001-content-backlog.md`. Step 1 (canonical attack-drill probe / FIVB 5.1 Stand and Spike) deferred to M002 scope per Root B feasibility blockers (no `attack` in `SkillFocus`, no selection path, D143 forbids `chain-attack`, source-backed pattern preconditions not met) — any future attack content requires its own decision packet (D148-shape) with the schema work it actually requires. Precedent boundary: executive close form bound to D130 founder-use-mode (single-user-team only); future milestone closures need their own decision + adversarial-memo amendment. **No app code changes; no schema changes; no edits to adversarial memo or cap-status JSON.** `docs/status/m001-validation-overhang.md` transitioned to `status: superseded`; `M001` milestone doc transitioned to `stage: complete` + `status: complete`; AGENTS.md / catalog.json / roadmap.md / current-state.md updated in the same atomic commit per decision-debt-sweep pattern. Originating round of doc-review on the requirements doc: 5 reviewers, 35 findings round 1 → 29 round 2 → 0 P0 after rewrite; plan-side doc-review caught 1 validator blocker (`superseded_by: D147` would have failed `successor_metadata_must_be_consistent`) + 6 canonical-drift gaps and applied them best-judgment before commit. Provenance: founder chat 2026-05-27 + `docs/research/2026-05-27-attack-content-and-solo-friction-feedback.md` + `docs/ideation/2026-05-27-attack-track-and-m001-closure-shape-ideation.md` + `docs/brainstorms/2026-05-27-attack-track-action-stack-requirements.md`.
- 2026-05-23: **Week-5 catch-up validation ritual landed; two M001 validation gates closed clean; Tier 2 polish + Phase 2B capture shapes unlock** (`docs/plans/2026-04-20-m001-adversarial-memo.md` `## 2026-05-18 (week 5 of 13)` catch-up entry). Performed 5 days late from the 2026-05-18 Monday; consecutive-miss clock at 1 of 3 (memo line 154 fires at 3). **D134 Phase 2A optional-streak falsification gate (2026-05-12 calendar read): CLEAN** — zero sessions in the 2026-04-28 → 2026-05-12 window ran a streak drill; both falsification gates' floors are 0; neither could fire. Phase 2B per-drill capture shapes (`points-to-target` / `pass-grade-avg` / `composite`) become pickable per `docs/status/post-m001-content-backlog.md`. **D130 Condition 3 partner unprompted-open final read (2026-05-21 calendar read): PASS** — 30-day quiet-window 2026-04-21 → 2026-05-21 demonstrated partner-return repeatedly (T+1 message, T+2 Dexie-instrumented open, five joint pair sessions, Seb-solo opens beyond T+1/T+2, positive partner signal on 2026-05-13 `GlossedText` affordance landing). Tier 2 polish unlocks per `docs/status/m001-validation-overhang.md` Phase Posture; Validation Posture String moves L2 → L3. **D130 Condition 2 (zero outside-app planning) attestation 2026-05-23: PASS for 2-player scope** — founder attested 3s/4s outside-app planning happened (forced by `D101` content gap; does not count under M001 scope); zero outside-app planning for 2-player sessions. Watch-item: founder reports "thinking about" outside-app planning for 2-player attack/tactics content (F2/F5 near-displacement leading indicator). **D130 trigger (e) agent-asymmetry: clean** — founder direct attestation of dozen+ independent app-use sessions with all Seb feedback from real field experience, reversing the 2026-05-10 "closest to firing" read. **A3 ligament re-read 2026-05-23** (founder-confirmed); next staleness date 2026-05-30; the 13-day staleness gap from prior 2026-05-10 read recorded as agent self-disclosure, not erased. **No code changes** — docs-only catch-up. `bash scripts/validate-agent-docs.sh` passes. **Consequence:** the M001 validation overhang now has two of four calendar-dated reads closed clean (D134 + D130 Condition 3), Condition 2 PASS attested, three of five early-re-eval triggers (a/b/e) clean, two on-the-clock (c soft / d UP at 7/10 strict-ledger), and Tier 2 polish + Phase 2B capture shapes both unlocked. The load-bearing new tension is the three-channel `D101` post-M001 sequencing signal (stated friction + observed session displacement + outside-app planning to compensate) plus an F2/F5 near-displacement leading indicator for 2-player attack/tactics content.
- 2026-05-22 / 2026-05-23: **Founder + Seb field-feedback capture and routing landed** (`docs/research/2026-05-22-mid-session-extend-and-content-asks-feedback.md`, plus cross-session reflections appended to `docs/research/founder-use-ledger.md`). Five named items captured: F1 Seb's 2026-05-16 mid-session voice-memo ask for an end-of-last-drill "extend with different focus" CTA (structurally novel surface not covered by any existing trigger, post-M001 backlog item, or open decision — single-source first-time evidence, on watch-for-second-hit posture); F2 attack drill section ask (content-gap-leaning, Tier 3+ post-M001); F3 custom session time (D135 feature-wish, second hit on 2026-05-09 90-min wish); F4 3+ player / 3s training (second hit on 2026-05-09 reflection item 1; sharpened 2026-05-23 to three-channel evidence including observed session-level displacement); F5 team play / tactics / blocking / positioning (one signal toward `D144` `pair_game` archetype activation triggers; BAB Plans 12-20 captured). 2026-05-23 addendum added: (a) 3+ player displacement-of-use (1×/week dip last week because second practice was 3s); (b) recent partner session ~2026-05-20 with export pending on Seb's device; (c) `GlossedText` affordance (commit `0d01803` 2026-05-13) hit with positive partner signal, closes 2026-05-04 F8/F9 wishlist loop; (d) Condition 2 attestation + F2/F5 near-displacement leading indicator; (e) trigger (e) clean attestation. No code changes; no Tier 1b cap consumption; no decision change. Provenance: 2026-05-22 + 2026-05-23 founder chat threads with Seb voice-memo content embedded.
- 2026-05-10: **Founder + Seb Pair + Net export incorporated into validation docs** (`docs/research/2026-05-10-pair-net-serving-duration-feedback.md`). Evidence update only — no app code or decision change. The session used a 40-min Pair + Net plan, ran 38 actual minutes, swapped main_skill to `d33-pair-open` Around the World Serving, and captured three drills (`d10-pair`, `d33-pair-open`, `d22-pair`) as `still_learning`. The app marked `ended_early` with wrap skipped, but the founder note says: "We did not end early - warmup timing still wrong. Too many minutes for drills." **Gate effects:** week-of-2026-05-04 closes at two logged sessions; founder/joint running total becomes 7; standard `D130` Condition 1 remains fail-trending (solo 1/7, set 0/3); early trigger (d) is now 7/10 with 0 partner-walkthrough P0s; `D134` does not move because no streak drill or metric capture ran. **Product read:** strengthens the warmup / duration-budget concern from the 2026-05-04 session and suggests 40-minute pair sessions may be over-budgeting drill blocks even when the user does not perceive the session as "ended early."
- 2026-05-09: **M001 build-complete relabel landed** (`docs/plans/2026-05-09-001-docs-m001-build-complete-relabel-plan.md`, status: complete). Paper-trail hygiene only — zero functional changes. **What landed.** M001 milestone frontmatter `stage` flipped from `validation` to `build-complete-validating` (free-form per existing catalog convention; no `status_vocabularies` change); `summary` extended to name the build-complete posture; new `## Phase Posture` section inserted near the top of the milestone doc body naming Build (complete 2026-05-08), Validation (active through 2026-07-20), Polish (Tier 2 deferred), and Trigger-gated content (parked). The "Current remaining M001 routing" paragraph collapsed from inline lists into pointers at two new status docs. **New docs.** `docs/status/m001-validation-overhang.md` is a single-page scoreboard enumerating every M001 validation gate (calendar reads 2026-05-12 / 2026-05-21 / 2026-07-20; `D130` Conditions 1/2/3; `D130` early re-eval triggers (a)–(e); `D134` Phase 2A streak gate). `docs/status/post-m001-content-backlog.md` is the trigger-gated content backlog (Tier 1b cap residual 4/10 consumed with the four reserved candidates; `d36` waiting on `O7`; `d43` waiting on `D101`; conditional Phase 2B per-drill capture shapes frozen until 2026-05-12). Both docs are pointer-only (cite source-of-truth IDs + line ranges; do not restate definitions). **Why this and not status: complete.** `complete` would lie about validation; the real state is "build done, validation active." The new stage value reads at a glance without overstating closure. M001 status stays `active`. **No M002 changes** — M002 activation is a separate next move. **Verification:** `bash scripts/validate-agent-docs.sh` passes; catalog updated with both new docs registered and M001 entry's `canonical_for` refreshed; both new docs follow the `docs/status/current-state.md` frontmatter pattern.
- 2026-05-08: **Bucket A1 `SourceBackedReroute[]` registry refactor shipped** (`docs/plans/2026-05-08-004-refactor-source-backed-reroute-registry-plan.md`, status: complete; commit `fb631dd`). Closes the last agent-actionable BAB-derived item from the 2026-05-04 ideation pass (Bucket A1; architecture-strategist explicit recommendation; the only purely-internal Bucket A item that did not depend on `O24` or T9). **What landed.** A new `app/src/domain/sessionAssembly/sourceBackedReroutes.ts` module (117 lines) owns four declarative `SOURCE_BACKED_REROUTES` entries (`d01-duration-fit`, `d47-d48-to-d49`, `d46-to-d50`, `d31-to-d51`) plus a single `shouldRerouteForSourceBackedSibling(slot, context, selected, plannedDurationMinutes)` predicate. Each entry records `id`, `description`, `fromDrillIds: ReadonlySet<string>`, `destinationDrillIds: readonly string[]` (metadata only — runtime targeting still goes through `pickForSlot(..., preferTargetDurationFit: true)`), optional `sessionFocus`, optional `playerLevel`, and optional `slotType` (defaults to `'main_skill'`). `app/src/domain/sessionBuilder.ts` shrinks by 93 lines: the three `shouldPreferAdvancedSettingDurationFit` / `shouldPreferAdvancedPassingDurationFit` / `shouldPreferBeginnerServingDurationFit` helpers and the three `ADVANCED_SETTING_DURATION_FIT_DRILL_IDS` / `ADVANCED_PASSING_DURATION_FIT_DRILL_IDS` / `BEGINNER_SERVING_DURATION_FIT_DRILL_IDS` constants and the inline `d01` reroute branch are all replaced with one registry call inside the existing redistribution branch. **Internal refactor only — byte-equivalent behavior.** No `BlockSlot`, `SessionArchetype`, layout, `selectArchetype`, route, schema, Dexie store, catalog data, archetype, or UI surface change. `SESSION_ASSEMBLY_ALGORITHM_VERSION` stays at 6; selection identity for any given seed is unchanged; the pinned golden-seed test stays green. **Coverage.** New `app/src/domain/sessionAssembly/__tests__/sourceBackedReroutes.test.ts` (336 lines) covers each registry entry's gate (focus / level / fromDrillId), data-integrity invariants (unique `id`, non-empty `fromDrillIds`, intentional empty `destinationDrillIds` for D01), capacity short-circuit, fatigueCap-pulled-below-planned edge cases, and the non-`main_skill`-slot defensive gate. Existing `sessionBuilder.test.ts` reroute coverage (D01 / D49 / D50 / D51 happy paths plus focus-gate and main-skill-only negative cases) passes without modification. **Future extensibility.** Adding a fifth source-backed reroute (the canonical near-future Tier 3+ extension; likely an Around-the-World-Serve-class non-`D101`-gated wrapper per Bucket A3 re-activation triggers) is now one entry append in the registry and zero changes to `sessionBuilder.ts` or to the predicate function. **Patterns doc updated.** `docs/solutions/2026-05-04-source-backed-content-depth-activation-pattern.md` reframes Required Boundary 4 around the registry as the new boundary, names the registry entry shape, and adds an `id` field to the implementation skeleton table; conceptual workflow is unchanged. **Verification:** typecheck clean, lint clean, full vitest suite green; ce-code-review autofixes pre-applied (corrected fatigueCap test name, demoted `SourceBackedReroute` to module-internal, added `id` field documentation to patterns doc). Closes the BAB ideation Bucket A1 row; Bucket B (B1/B2/B3) decision packets shipped same day as `D142` / `D143` / `D144` (next entry).
- 2026-05-08: **Pre-`D101` Bucket B schema decision passes landed as `D142` / `D143` / `D144`** (`docs/plans/2026-05-08-002-feat-bucket-b-pre-d101-schema-decision-passes-plan.md`, `docs/decisions.md` `D142` / `D143` / `D144`, plus three brainstorm packets at `docs/brainstorms/2026-05-08-002-b1-t9-scoring-overlay-modeling-stance-requirements.md`, `docs/brainstorms/2026-05-08-003-b2-t6-attack-zone-convention-default-requirements.md`, `docs/brainstorms/2026-05-08-004-b3-slot-4-movement-optionality-requirements.md`). Settles the three Bucket B decision-pass packets named in the 2026-05-04 BAB-complete plan-builder ideation as the next agent-actionable M001 follow-ups after `D141`. `D142` picks the T9 scoring-overlay-grammar modeling stance: sibling variants on a parent drill with composable overlay metadata, supporting rule-restriction and rule-augmentation symmetrically; asymmetric scoring (Plan 20) deferred indefinitely. `D143` picks the T6 attack-zone-convention default: FIVB 5-zone for chain-wide numbered-zone attack drills; BAB attack-accuracy boxes (Plan 7) reserved for per-shot-accuracy drills; mixing 5-zone and 7-zone inside one chain forbidden. `D144` decides slot-4 (movement) optionality: adopt a new `pair_game` archetype variant with a slot-4-skipped layout as the canonical shape (peer of `pair_net` / `pair_open`), not runtime layout compression of existing `pair_net`; implementation deferred behind partner-walkthrough `≥P1` / founder-ledger tournament-prep / `D101` triggers. **Decision-spine cleanup only.** The only app-tree edit is a comment-only addition to `app/src/data/archetypes.ts` citing `D144` next to the existing `D141` invariant block; no `BlockSlot`, `SessionArchetype`, layout, `selectArchetype`, route, schema, catalog data, UI surface, or diagnostic-classifier change. Synthesis (`docs/research/practice-plan-authoring-synthesis.md` T6 / T9 / slot-4 cluster-complete row), the BAB ideation (Bucket B B1 / B2 / B3 entries), the milestone routing, and the catalog all cross-reference the new decision rows. Authorization boundary on every row: schema implementation requires its own future plan with its own authorization, gated by `D101` unlocks, `M002` plan-grammar input, or a non-`D101`-gated source-priority drill clearing diagnostics activation (per `D142`); `D101` unlocks plus the first attack-chain candidate (per `D143`); or any of the partner-walkthrough / founder-ledger / `D101` triggers (per `D144`).
- 2026-05-08: **M001 milestone refresh and `O24` resolution landed as `D141`** (`docs/plans/2026-05-08-001-refactor-m001-o24-decision-spine-plan.md`, `docs/decisions.md` `D141`). The M001 milestone now reflects the current Setup -> Safety pre-run spine from `D137`, the shipped Tier 1a / Tier 1b Layer A state, and the remaining evidence-gated work. `O24` is resolved: M001 keeps the single-skill-chain generation constraint for focus-controlled slots, but that implementation constraint is not a permanent product principle that future focused sessions must be skill-isolation-only. No runtime behavior, app routes, catalog schema, Dexie schema, UI surface, or generated-plan diagnostics changed. Future integrative-focus or `compatibleFocuses` work remains blocked behind its own plan and the revisit triggers named in `D141`.
- 2026-05-07: **`D140` D49 generator-policy proposal packet authored; the named "next artifact" of the 2026-05-03 U8 proof packet is now committed as a docs+diagnostic-packet artifact** (`docs/plans/2026-05-07-004-feat-d49-generator-policy-proposal-plan.md`, `docs/decisions.md` `D140`). Closes the gap left by the 2026-05-03 U8 proof, which had emitted `ready_for_generator_policy_proposal` (20 pressure-disappears cells, 0 pressure-remains cells) but left the proposal direction uncommitted across regenerations. **What landed.** A new `D49 Generator-Policy Proposal Packet` in `app/src/domain/generatedPlanDiagnosticTriage.ts` (`buildGeneratedPlanD49GeneratorPolicyProposalPacket` + `formatGeneratedPlanD49GeneratorPolicyProposalPacketMarkdown`) consumes the existing U8 proof packet, selects `cap_redistribution_at_carrier_max` as the proposed direction when the upstream proof is `ready_for_generator_policy_proposal`, defers to `no_action_yet` otherwise, and renders into the triage workbench markdown adjacent to the existing `## D49 U8 Generator-Policy Proof` section. The packet records three rejected alternatives (`status_quo_with_policy_allowance`, `preferential_in_band_reroute`, `early_block_truncation`) with rationale, a falsification threshold, and a revisit trigger. **No app surface change.** Every change-authorization field stays `not_authorized`: `runtimeRedistribution`, `catalog`, `cap`, `sourceDepth`, `d47Reopen`. `buildDraft()`, optional-slot redistribution, drill candidate scoring, drill metadata, D49 caps, D47 closed-by-D49 state, and the U8 proof's outputs are unchanged. **Scope.** D49-only (`scope: 'd49_only'`); the analogous packets for non-D49 `likely_redistribution_caused` groups (D33, D40, D51, D22, D50) require their own U8 proofs first. **Net change.** ~80 lines added to `generatedPlanDiagnosticTriage.ts`; 1 line added to `app/scripts/validate-generated-plan-diagnostics-report.mjs` dependency list; 22 lines added to the regenerated triage; 5 new tests in `app/src/domain/__tests__/generatedPlanDiagnosticTriage.test.ts` pin direction, deferral, authorization invariant, and markdown rendering. **Verification:** typecheck clean, lint clean, generatedPlanDiagnosticTriage test file 89/89 green, full app test suite to be run as the final verification step.
- 2026-05-07: **`D139` validator-script CI gating resolved; `architecture:check --strict`, `typography:guardrails:check`, and `diagnostics:report:check` are now CI gates** (`docs/plans/2026-05-07-003-d139-validator-script-ci-gating-plan.md`, `docs/decisions.md` `D139`). Resolves the `D139` slot reserved by `D137`. Investigation: four validator-style scripts exist beyond the standard `tsc -b` / `eslint` / `vitest` / `prettier --check` / Playwright. Two were already gated by `agent-validation.yml` (`scripts/validate-agent-docs.sh`, `scripts/test-validate-agent-docs.sh`); three were not (`architecture:check`, `typography:guardrails:check`, `diagnostics:report:check`). All three are sub-second, all green today. Resolution: add three new steps to `.github/workflows/app-ci.yml`'s `build-and-lint` job between `Lint` and `Test`, ordered cheap → expensive (architecture → typography → diagnostics). Total CI overhead added: ~1.1s. **Strict mode for architecture.** `architecture:check -- --strict` fails on `level: 'error'` findings only; the existing 21 `screenComponentServiceImport` advisories (level `advisory`) report but do not fail — controller-debt triage stays untouched, future hard layer-boundary violations block PRs at review time. **No npm-script compression.** `diagnostics:triage:check` is byte-identical to `diagnostics:report:check` because the underlying script validates both surfaces in one run; alias exists for keyword discoverability and harms nothing. **Friction acknowledged.** `diagnostics:report:check` is content-coupled — drill-catalog or generator changes that drift the report require running `npm run diagnostics:report:update` before pushing; the gate makes the freshness contract enforced rather than aspirational. **No pre-commit hook added** (Cursor-first repo, founder-use mode prefers post-push CI feedback over pre-commit friction). **Verification:** typecheck clean, lint clean, all three new gates pass green locally (architecture --strict: 21 advisories / 0 errors / exit 0; typography: passed; diagnostics: report + triage current); 150/150 test files / 2116/2116 tests passing; agent-doc validator passed.
- 2026-05-07: **`D138` diagnostic-spine canonicality resolved; `focusReadiness` retired except as shared dimensions** (`docs/plans/2026-05-07-002-d138-diagnostic-spine-canonicality-plan.md`, `docs/decisions.md` `D138`). Closes the second open follow-up from the 2026-05-05 merge plan ("reconcile feat's `focusReadiness` diagnostic output with the new `focusCoverageAudit`"). Investigation showed the overlap was "two parallel implementations where one was already retired by attrition" — `focusCoverageAudit` is the live detection diagnostic (snapshot regression test, markdown report at `docs/reviews/2026-05-04-focus-coverage-audit.md`, walks the user-facing 5-tier `SkillLevel`); `focusReadiness`'s audit logic and gap-card / activation-manifest remediation API were a parallel implementation whose only remaining consumers were its own tests. Resolution: prune `focusReadiness.ts` to the dimension constants and types still imported by `app/src/domain/generatedPlanDiagnostics.ts` (`VisibleFocus`, `ReadinessConfiguration`, `ReadinessConfigurationId`, `VISIBLE_FOCUSES`, `PLAYER_LEVELS`, `READINESS_DURATIONS`, `READINESS_CONFIGURATIONS`); delete `focusReadiness.test.ts` (the dimension constants are exercised through `generatedPlanDiagnostics.test.ts`); rewrite the file doc-comment to declare its narrower role and explain why the speculative remediation API was retired so future agents do not silently restore an unmaintained surface. **Net change.** ~400 lines removed from `focusReadiness.ts`, ~325 lines removed by deleting `focusReadiness.test.ts`. Zero behavior change in any live diagnostic; zero UI surface change. **Verification:** typecheck clean, lint clean, 150/150 test files / 2116/2116 tests passing (was 151/151 / 2133/2133 — the 17-test delta is exactly the deleted self-test); `npm run diagnostics:report:check` and `npm run diagnostics:triage:check` both green; `focusCoverageAudit` snapshot unchanged at 180/180 covered; `bash scripts/validate-agent-docs.sh` passed. **Future-proofing.** If a remediation tracker becomes worth building, re-author from the new context (likely a JSON gap registry under `docs/`, or a typed module composed against whichever live diagnostic is being remediated) rather than restoring the previous file shape — those types were speculative pre-implementation. `D139` (validator script gating / compression) remains separately deferred.
- 2026-05-07: **Engine two-pass band relax restored in `pickForSlot`** (`docs/plans/2026-05-07-001-engine-two-pass-band-relax-plan.md`). Closes the "skill stuff fix" follow-up left open by the 2026-05-05 merge plan. When a focus-controlled REQUIRED slot has exhausted its in-band UNUSED candidates, `pickForSlot` now prefers an out-of-band UNUSED drill of the same focus over duplicating an in-band drill. Engine-only — no UI surface, no `levelRelaxed` field, no copy hint; D137 is preserved. The audit and the runtime engine now share `partitionByLevel` as the single band-membership predicate; `partitionByLevel.ts`'s doc-comment was refreshed to list current consumers (audit + relax pass) instead of the pre-merge two-pool prose. **Behavioral consequence:** the narrowest cell in the focus-coverage audit (`competitive_pair × serve`, where the advanced-band serve drill set is `{d22, d33}` — d23 is `m001Candidate: false`, d51 is `main_skill`-only) no longer silently picks the same drill twice across `technique` and `main_skill` on a layout that has more focus-controlled required slots than in-band drills. **Test coverage:** three new unit tests in `app/src/domain/sessionBuilder.test.ts` pin (1) the positive relax path, (2) the no-relax-when-in-band-unused-exists negative case, and (3) the optional-slot guard (`allowUsedFallback: false` still returns `undefined`). **Verification:** typecheck clean, lint clean, 151/151 test files / 2133/2133 tests green; focus-coverage audit snapshot unchanged at 180/180 covered. The 2026-05-05 merge entry's "Follow-up 'skill stuff fix'" line is now resolved; reconciling `focusReadiness` and `focusCoverageAudit` diagnostic surfaces is still tracked as a separate follow-up under `D138`.
- 2026-05-06: **D137 Tune today route cleanup landed** (`docs/decisions.md` D137; `docs/plans/2026-05-06-001-refactor-d137-tune-today-routing-cleanup-plan.md`). Setup -> Safety is now the canonical pre-run route; `/tune-today`, `TuneTodayScreen`, the Tune today controller, `regenerateDraftFocus`, and `SessionDraft.levelRelaxed` are removed. Home draft-start / Repeat / Repeat-what-you-did paths now route directly to Safety after saving a draft. `/settings/skill-level` remains the durable manual skill-level override, now marks the current saved level and Settings keeps a correction path for migrated/backfilled users with no saved level. `ChoiceSubsection` owns the conditional setup reveal transition. The Focus Coverage Audit bucket formerly named `level_unhonored` is now `cannot_generate_at_level`; the generated audit remains 180/180 covered with 0 failing cells. D138 diagnostic-spine canonicality and D139 validator script gating/compression remain deferred follow-ups.
- 2026-05-05: **`feat/focus-coverage-readiness` merged into `main`; single-branch flow adopted** (`docs/plans/2026-05-05-001-merge-focus-coverage-and-collapse-branches-plan.md`, status: complete). 25 unmerged commits from `feat/focus-coverage-readiness` (focus-readiness diagnostics, ChoiceSection UI primitive layer, ChoiceRow/ChoiceSubsection/ScreenHeader extractions, default-focus Setup reroute back to `/safety`, source-backed activation pattern docs, generated-plan-diagnostics, and richer drill catalog) merged via `git merge --no-ff -X theirs`. **Lossy outcomes accepted (per user direction "we fix the skill stuff after"):** the d135 two-pass `pickForSlot` / `pickMainSkillSubstitute` mechanism with `effectiveLevelValue` parameter was overwritten in favor of feat's `playerLevel` hard-filter design (`d67f65d feat: apply skill level to focused generation`); `effectiveLevel` and `partitionByLevel` primitives are kept (consumed by the Focus Coverage Audit only); `SessionDraft.levelRelaxed` field + `useTuneTodayController` plumbing + Tune today eyebrow render survived the merge but are **functionally dead** because feat's `pickForSlot` does not produce a relaxation signal; re-wiring is the explicit follow-up below. **Preserved main-only adds (intact):** `SkillLevelPicker` shared component, `SettingsSkillLevelScreen` + sub-route at `/settings/skill-level`, Settings section + always-on fallback row, `focusCoverageAudit` module + Vitest snapshot test (snapshot rebaselined post-merge: 180/180 covered, 0 failing — the merged richer catalog closed every cell that was failing pre-merge). **Branch hygiene:** `feat/focus-coverage-readiness` and `feat/warmup-wrap-segment-snap` (already merged-equivalent) deleted local + remote; backup tag `pre-focus-coverage-merge-2026-05-05` pushed for rollback. **Single-branch policy adopted in `AGENTS.md`:** all work happens on `main`, push after every commit, no long-lived feature branches. **Verification:** `npx tsc -b --noEmit` clean; `npm run lint` clean; targeted tests on focusCoverageAudit / SettingsScreen / SettingsSkillLevelScreen / SkillLevelPicker / SkillLevelScreen / TuneTodayScreen / SetupScreen / sessionBuilder / regenerateDraftFocus / findSwapAlternatives all green (171/173 — 2 stale-snapshot failures rebaselined). **Follow-up "skill stuff fix":** re-wire two-pass relax in feat's `pickForSlot` (or equivalent) so `levelRelaxed` actually fires for `competitive_pair` users on serving/setting sessions; reconcile feat's `focusReadiness` diagnostic output with the new `focusCoverageAudit` (overlap in intent, different artifacts).
- 2026-05-04: **BAB-drill-book-complete ideation pass landed** (`docs/ideation/2026-05-04-bab-complete-plan-builder-ideation.md`, status: active). After the captured BAB drill book reached structural completeness, a `ce-ideate` pass dispatched 6 ideation frames + 2 adversarial-critique subagents (`ce-architecture-strategist`, `ce-product-lens-reviewer`) to evaluate how Volleycraft should build practice plans. **Load-bearing finding:** the BAB synthesis is comprehensive **research** but should not drive plan-builder direction at this **milestone**. D101 gates 5 of 6 BAB top-tier source-priority drill families; D130 founder-use mode has 17 days remaining to the 2026-05-21 condition read-out; M001 is solo/pair-first; the calm-courtside identity argues against importing BAB-grade schema complexity before content can ship. The synthesis should be treated as **archived inventory + structural confirmation of existing canon**, not a build agenda — re-activation triggers are now explicit in the synthesis frontmatter (D101 unlocks; M002 needs plan-grammar input; or a non-D101-gated source-priority drill clears the existing `d49 / d50 / d51` diagnostics activation pattern). **Confirmation receipts (canon-validating BAB findings):** T1 confirms the integrative-focus reading flagged in `O24` (does not by itself resolve `O24`); T2 confirms `D76` feed-type honesty; T5 confirms `D77` fatigue cap policy; T7 confirms `D101` 3+ player participant honesty; spine generalization (6-slot focus-agnostic plan grammar across all five captured clusters) externally validates the existing `warmup → technique / movement_proxy → main_skill → pressure → wrap` archetype shape. **Survivor buckets** (~46 raw candidates → 12 survivors after adversarial filtering): **Bucket A — Now-shippable (4):** A1 refactor `shouldPrefer*DurationFit` predicates into a `SourceBackedReroute[]` registry (3 validated applications); A2 receipt the canon-validating BAB confirmations; A3 re-activation triggers in synthesis frontmatter (landed in this pass); A4 small `O24` decision-pass packet. **Bucket B — Pre-D101 schema gates (3 decisions):** B1 pick a T9 modeling stance; B2 pick a T6 zone convention; B3 decide whether slot 4 (movement) becomes optional in archetype contracts. **Bucket C — Post-D101 / M002 candidates (5; gated by re-activation triggers):** C1 composable scoring grammar; C2 problem-first plan flow (training-problem axis); C3 drill family abstraction (`familyId` axis); C4 repetition-rank-aware catalog metadata; C5 `compatibleFocuses` axis on drill records. **Sequencing recommendation:** the single most important call is to resolve `O24` before any schema work — it gates the read-drill schema, `compatibleFocuses`, and the integrative-focus interpretation. Smallest-cost-highest-value sequence (~2 days total): A2 + A3 + A4 as a single status / decisions pass; A1 as an internal refactor; B1 + B2 + B3 as decision-pass brainstorms. **All of Bucket C waits for explicit re-activation triggers.** **Anti-patterns to avoid importing:** per-team rules without session-context model; per-shot drills as duplicate records; universal warm-up opener; credit-inflating BAB scoring grammar over FIVB Modified Games; read drills as overloaded `pass`/`set` scenarios; alias-aware schema. **Rejected:** importing BAB plan generation wholesale into M001; adding BAB-grade complexity to calm courtside UI before D101; authoring 3+ player drills before `D101` unlocks; per-team rules without session-context model; universal warm-up opener as Volleycraft default; read-drill 3-axis schema before D135 evidence. Synthesis doc (`docs/research/practice-plan-authoring-synthesis.md`) updated with new `Ideation Pass Receipt` section and re-activation triggers in frontmatter / Quick Scan. Routing updated in `docs/research/README.md` and `docs/catalog.json`. **A3 (re-activation triggers) shipped in this pass; A4 was resolved by `D141` on 2026-05-08; A1, A2, B1, B2, B3 remain to be picked up if the team wants to act.**
- 2026-05-04: **BAB Practice Plan 20 (Game Play) integrated into the BAB source archive (`docs/research/bab-source-material.md`) — completes the captured BAB Drill Book** (all 20 plans now captured at source-detail level). Plan 20 is the fourth and final Game Play plan, completing the captured Game Play cluster (Plans 17-20). Partial source capture: Drill 1 (Best Warm Up Ever) name-only; Drill 2 (Triangle Setting) truncated source text — first Triangle Setting source-text capture since Plan 14, structurally consistent with the 4-player hit-fed source form; Drills 3-6 captured (some truncated). Plan 20 contributes **one structurally novel drill, the fourth T9 sharpening in four consecutive captures, the cluster-complete confirmation of two structural patterns, and four major alias-chain extensions**: (1) **`16-18 Run with It`** introduces a **ninth composable scoring rule kind** to the captured BAB grammar — *reset-on-miss / consecutive-streak-required scoring* — Team A starts at 16, must reach 21 by scoring **consecutively**, missing a point resets the score to 16 (Team B accumulates points normally). Distinct from existing `successMetric.type: streak` (no numeric base/target structure) and from the eight earlier-captured rule kinds. Future Volleycraft `successMetric` taxonomy needs a new `streak-required-with-floor-reset` metric shape; (2) **First BAB source for asymmetric scoring** — different teams have different scoring rules in the same drill (Team A reset-on-miss, Team B accumulating-normal). **First per-team-rule mechanism** in the captured BAB scoring grammar — distinct from any earlier captured rule kind because earlier rules applied symmetrically to both teams. Future Volleycraft scoring metadata needs an asymmetric-scoring axis: `scoringRules: { teamA: ..., teamB: ... }` rather than a single drill-level rule. **Tenth structural axis** in the T9 grammar; (3) **Densest single-drill scoring grammar in the captured set** — Plan 20 Drill 5 composes four rule kinds (reset-on-miss + asymmetric + win-by-2 + side-switch) on a single drill; (4) **Triangle Setting (4p hit-fed) and 14-15 Games to 21 each extend to eight plan references** (tied as the most-repeated source forms in the captured set); Around the World (Serve) extends to four plan references (Plans 1, 4, 8, 20); `3 Before 5` family extends to five plan references across three clusters (passing 1/4, attacking 8 with `SHOTS ONLY`, game-play 17 with `BIG point`, game-play 20 base form); Plan 20 Drill 2's truncated source text is the first Triangle Setting source-text capture since Plan 14, structurally consistent with the 4-player hit-fed source form; (5) **Slot-4-skip pattern cluster-complete-confirmed across all four Game Play plans** (17, 18, 19, 20 all skip slot 4 movement) — the slot-4-skip is now a strongly-confirmed Game Play structural feature, not a partial pattern; (6) **T10 (read drills) narrowed from universal pattern to Defense/Attack-only pattern** — all four Game Play plans contain zero read drills, cluster-complete confirmation that read drills do not recur in pure Game Play. The four-cell read-drill taxonomy (Plans 9, 11, 14, 16) appears complete for the captured BAB set; (7) **Within-Game-Play slot 2 primer confirmed as "any 4-player chain primer"** — Plans 17, 18, 20 use Triangle Setting; Plan 19 uses Cross Court Pepper (3 of 4 Triangle Setting; 1 of 4 Cross Court Pepper). Future Game Play archetypes should support multiple primer choices with Triangle Setting as the default; (8) **Within-Game-Play slot 3 confirmed as spanning all four sub-skills** — Plan 17 attacking accuracy, Plan 18 setting under serve, Plan 19 attacking accuracy with blocker, Plan 20 serving accuracy; (9) **T9 sharpened four consecutive times in four consecutive plan captures** (Plan 17 from two-pattern symmetry to six rule kinds; Plan 18 from six to seven; Plan 19 from seven to eight; Plan 20 from eight to nine plus asymmetric axis) — `Theses Retired or Narrowed` section now has seven real falsification entries; the rate of T9 sharpening has been consistent across the entire Game Play cluster. **The captured BAB drill book is now structurally complete at source-detail level** (~30 distinct source forms accounting for ~140 captured drill slots across 20 plans). Future BAB-related Volleycraft work should treat the captured set as the authoritative reference. Practice-plan authoring synthesis updated with: Quick Scan reflecting 20-of-20 captured at source-detail; Captured Plan Grammar Templates section's Game Play column extended with Plan 20 content (slot 4 cluster-complete-skipped, slot 3 spans all four sub-skills); T9 evidence with fourth sharpening recorded in `Theses Retired or Narrowed`; T10 evidence narrowed to Defense/Attack-only with cluster-complete confirmation; T13 evidence updated to include Plan 20's confirmation and the new repetition-rank tier (Triangle Setting and 14-15 Games at 8 plans, Comp Transition at 6, Cross Court Pepper at 5, `3 Before 5` at 5, Around the World Serve at 4, Best Warm Up Ever at 18); Bottom Line and frontmatter summary updated. BAB source archive updated with Plan 20 source capture, Plan 20 synthesis section, vocabulary glossary entries (`16-18 Run with It`, `Reset-on-miss scoring`, `Asymmetric scoring`, `Streak-with-floor-reset`, `Game Play cluster (complete)`, `Captured BAB drill book (complete)`), and Source-name cross-reference table extended with Triangle Setting (4p hit-fed) at eight plan references, 14-15 Games to 21 at eight plan references, `3 Before 5` family at five plan references, Around the World (Serve) at four plan references, and Plan 20 Drill 5 entry. Routing updated in `docs/research/README.md` and `docs/catalog.json`. No code or canon changes; all Plan 20 novel content remains Tier 3+ game-play / pressure-game wrapper candidates. **This integration completes the BAB Drill Book capture.**
- 2026-05-04: BAB Practice Plan 19 (Game Play) integrated into the BAB source archive (`docs/research/bab-source-material.md`). **Third Game Play plan captured at source-detail level** following Plans 17 and 18; the founder correctly labels Plan 19 as "game play." Partial source capture: Drill 1 (Best Warm Up Ever) is name-only; Drills 2–6 are captured in full. Plan 19 contributes **one structurally novel drill, the third T9 sharpening in three consecutive captures, and four additional alias-chain extensions**: (1) **`Small Court: Mini Games to 7`** introduces an **eighth composable scoring rule kind** to the captured BAB grammar — *scoring-zone gate* — where points only score when the rally terminates with a specific spatial outcome (the ball must land on the designated half-court for the rally to count). Distinct from rule-restriction (which constrains *behavior* — players still hit anywhere; only the outcome zone matters); fifth Mini Games to 7 variant in the captured set; also has a level-scaled rule (beginner/intermediate any-player serve; advanced same-player serve), which graduates Thesis T12 (level-scaled rules) from sample-of-one to two-source pattern; (2) **Comp Transition HL/CS Live extends to six plan references** (Plans 7, 10, 12, 13, 14, 19) — now the **most-aliased drill in the captured BAB set**; (3) **Cross Court Pepper extends to five plan references** (Plans 5, 7, 11, 16, 19) — third-most-repeated drill family; **Plan 19 is the first Game Play plan to use Cross Court Pepper as the slot 2 primer**, narrowing the within-Game-Play grammar template's slot 2 from "Triangle Setting only" (Plans 17/18) to "any 4-player chain primer"; (4) **+3/-3 Highlines base form extends to two plan references** (Plans 7, 19) — Plan 11 Drill 4 is the rule-augmented sibling (with-blocker -1 penalty); (5) **`Game to 21 (no regulations)`** is the cleanest minimal-overlay base form of the Games to 21 wrapper family — confirms BAB has both base forms (this) and overlay variants (everything else captured), supporting a base-plus-composable-overlays modeling approach; (6) **Slot-4-skip pattern strongly confirmed across three consecutive Game Play plans** (Plans 17, 18, 19 all skip slot 4 movement) — three consecutive confirmations strongly support the slot-4-skip as a Game Play structural feature, not a Plan 17 anomaly; Plan 20 remains the final cluster-level test; (7) **T9 sharpened three consecutive times in three consecutive plan captures** (Plan 17 from two-pattern symmetry to six rule kinds; Plan 18 from six to seven by adding conjunctive/wash; Plan 19 from seven to eight by adding scoring-zone gate) — `Theses Retired or Narrowed` section now has five real falsification entries, the synthesis is now actively narrowed by new evidence with each capture; (8) **Three-of-four confirmed cluster framings of T1 (drill-repurposing across focus) within the Game Play cluster**: Plan 17 reuses `3 Before 5` (passing-focus origin); Plan 18 reuses `Triangle Setting (Serve)` (setting-focus origin); **Plan 19 reuses `+3/-3 Highlines (CS / HL)` (attacking-focus origin)**; T1 is now confirmed across all five captured cluster framings. Practice-plan authoring synthesis updated with: Quick Scan reflecting 17-of-20 capture state; Captured Plan Grammar Templates section's Game Play column extended with Plan 19 content (slot 2 primer narrowed); T9 evidence with third sharpening recorded in `Theses Retired or Narrowed`; T12 evidence updated with second source case; T13 evidence updated to include Plan 19's confirmation and the new repetition-rank tier (Comp Transition at 6 plans, Cross Court Pepper at 5 plans); Bottom Line and frontmatter summary updated. BAB source archive updated with Plan 19 source capture, Plan 19 synthesis section, vocabulary glossary entries (`Small Court: Mini Games to 7`, `Scoring-zone gate`, `Game to 21 (base form)`, `Within-Game-Play slot 2 primer`), and Source-name cross-reference table extended with Cross Court Pepper at five plans, Comp Transition at six plans, +3/-3 Highlines base form at two plans, and Plan 19 Drills 5-6 entries. Routing updated in `docs/research/README.md` and `docs/catalog.json`. No code or canon changes; all Plan 19 novel content remains Tier 3+ game-play / pressure-game wrapper candidates.
- 2026-05-04: BAB Practice Plan 18 (Game Play) integrated into the BAB source archive (`docs/research/bab-source-material.md`). **Second Game Play plan captured at source-detail level** following Plan 17 (the founder correctly labeled this plan "game play" — Plan 17's "defensive" mislabel resolved). Partial source capture: Drills 1 (Best Warm Up Ever) and 2 (Triangle Setting) are name-only; Drills 3–6 are captured in full. Plan 18 contributes **two structurally novel drills, the second sharpening of T9 in two consecutive captures, and the first Game Play structural-feature confirmation across two data points**: (1) **`Triangle Setting with Attack (Serve)`** is the **first whole-chain Triangle Setting variant** captured — P1 serves, P2 passes AND hits the set, P3 and P4 set; the single-passer-attacker workload pattern is beach-realistic because the receiving team has only 2 players. New chain-completion sibling variant on the Triangle Setting family; argues for participant-role-transition as a future drill metadata axis (the same player contributes two contacts in different roles within a chain); (2) **`Add In/Add Out`** is the **first BAB source detail for wash-drill scoring** — points only score when both halves of a serve-then-receive cycle are won by the same team; if conditions split, the result "washes" and no points score. Adds a **seventh composable scoring rule kind** to the captured grammar (conjunctive/wash with cancellation) — distinct from rule-restriction, rule-augmentation, tiebreaker, side-switch trigger, outcome-elevation gate, and conditional-extension-if-time-allows; (3) **Slot-4-skip pattern confirmed across two consecutive Game Play plans** (Plans 17 and 18 both skip slot 4 movement) — narrows the captured BAB plan grammar's slot 4 from "always present" to **optional**, and confirms the slot-4-skip is a Game Play structural feature, not a Plan 17 anomaly; (4) **Triangle Setting (4p hit-fed) and 14-15 Games to 21 are now tied at seven plan references each** as the most-repeated source forms in the captured set — both are now the source-priority leaders per Thesis T13 (repetition signals priority); (5) **`Triangle Setting (Serve)` from Plan 6 Drill 4 is now confirmed as a recurring source form** with Plan 18 Drill 3 — extending the cross-reference for `d44` to two BAB plan references; (6) **T9 sharpened twice in two consecutive plan captures** (Plan 17 from two-pattern symmetry to six rule kinds; Plan 18 from six to seven by adding conjunctive/wash with cancellation) — `Theses Retired or Narrowed` section now has four real falsification entries; the rate of T9 sharpening across consecutive captures is itself a signal that the captured BAB scoring grammar has multiple independent composable axes that didn't surface until enough source cases accumulated. Practice-plan authoring synthesis updated with: Quick Scan reflecting 16-of-20 capture state; Captured Plan Grammar Templates section's Game Play column extended with Plan 18 content; T9 evidence with second sharpening recorded in `Theses Retired or Narrowed`; T13 evidence updated to include Plan 18's confirmation and the new repetition leaders; Bottom Line and frontmatter summary updated. BAB source archive updated with Plan 18 source capture, Plan 18 synthesis section, vocabulary glossary entries (`Add In/Add Out`, `Wash drill (full source)`, `Conjunctive / wash scoring`, `Triangle Setting (Serve)`, `Triangle Setting with Attack (Serve)`, `Single-passer-attacker workload pattern`, `Game Play cluster grammar`), and Source-name cross-reference table extended with Triangle Setting (4p hit-fed) at seven plan references, 14-15 Games to 21 at seven plan references, Plan 6 Drill 4 / Plan 18 Drill 3 alias, and Plan 18 Drills 4-5 entries. Routing updated in `docs/research/README.md` and `docs/catalog.json`. No code or canon changes; all Plan 18 novel content remains Tier 3+ game-play / pressure-game wrapper candidates.
- 2026-05-04: BAB Practice Plan 17 (Game Play) integrated into the BAB source archive (`docs/research/bab-source-material.md`). **First Game Play plan captured at source-detail level** — the founder labeled the plan "defensive" in the paste, but the drill content (Race to 5 HL and CS competitive accuracy race, 10 Sideouts per-player sideout-rate measurement format, 3 Before 5 with new BIG point outcome-elevation overlay, Games to 21 full match with conditional-extension rule) is unambiguously game-play and matches BAB's drill-book cluster ordering where Plans 17–20 form the Game Play cluster following the Defense cluster (Plans 12–16). Partial source capture: Drills 1 (Best Warm Up Ever) and 2 (Triangle Setting) are name-only; Drills 3–6 are captured in full. Plan 17 contributes **three structurally novel drills, one new outcome-elevation overlay on an existing drill, and the first real falsification entries** in `docs/research/practice-plan-authoring-synthesis.md`'s previously-empty `Theses Retired or Narrowed` section: (1) **`Race to 5 HL and CS`** is the first BAB drill with **two role-symmetric competing attackers** (both left sides racing to 5 in-the-box accuracy shots) and the first BAB drill using race-to-N format for accuracy — argues for a `participantStructure` schema axis distinguishing role-differentiated, role-symmetric-cooperative, and role-symmetric-competitive structures; (2) **`10 Sideouts`** is the first BAB drill explicitly using FIVB Level I Coaches Manual Ch VII's "successful reps in a fixed trial count" measurement mode — strongest cross-source convergence to date with FIVB's measurement-mode taxonomy; (3) **`3 Before 5` with `BIG point` overlay** is the first BAB capture of a **fourth scoring-overlay rule kind** (outcome-elevation, where achieving the win condition triggers a sub-game whose result is what scores) — sharpens T9 from "two-pattern symmetry" (rule-restriction ↔ rule-augmentation) to "at-least-six composable rule kinds" (adding tiebreaker from Plan 16, side-switch trigger, outcome-elevation gate, and conditional-extension-if-time-allows from Plan 17); (4) **`Games to 21`** introduces the **conditional-extension scoring rule** (final set to 15 if time allows, regardless of outcome) — composable with all other rule kinds; (5) **`3 Before 5` family alias chain extends to four plans across three focus clusters** (passing in Plans 1/4, attacking in Plan 8 with `SHOTS ONLY`, game-play in Plan 17 with `BIG point`), now the highest-repetition-rank source-priority drill in the captured set per Thesis T13. **Spine generalization partially confirmed against Game Play:** Plan 17 fits five of the six captured spine slots (opener / primer / per-shot technique / decision-or-pressure overlay / live-feed transfer / competitive wrapper) and intentionally skips slot 4 (movement) — sharpens slot 4 to **optional** in future Volleycraft session-builder logic and slot 5 to a generic **decision-or-pressure overlay** slot rather than a read-only slot. Plan 17 also confirms the adversarial-review prediction that Game Play would test the spine, but **does not falsify** the spine claim — it instantiates it with a structurally honest slot-4 skip. Practice-plan authoring synthesis updated with: Quick Scan reflecting 15-of-20 capture state; Captured Plan Grammar Templates section extended with a Game Play column; T9 evidence and sharpening recorded in `Theses Retired or Narrowed` (the previously-empty slot now has its first three real entries); T13 evidence updated to include Plan 17's confirmation; Bottom Line and frontmatter summary updated. BAB source archive updated with Plan 17 source capture, Plan 17 synthesis section, vocabulary glossary entries (Race to 5 HL and CS, 10 Sideouts, BIG point, Side-switch trigger, Outcome elevation, Conditional extension, Match to 21, Game Play cluster, Participant structure), and Source-name cross-reference table extended with Plan 17 entries plus the four-plan `3 Before 5` alias chain. Routing updated in `docs/research/README.md` and `docs/catalog.json`. No code or canon changes; all Plan 17 novel content remains Tier 3+ game-play / pressure-game wrapper candidates.
- 2026-05-04: practice-plan authoring synthesis comprehensive pass after the Defensive cluster completed (`docs/research/practice-plan-authoring-synthesis.md`, status: active). Builds on the incremental T1-T12 evolution from Plans 1-16 with five structural additions designed to consolidate plan-builder findings now that 14 of 20 BAB plans are captured, **and to install epistemic-honesty guardrails after an adversarial review surfaced ten substantive objections**: (1) **`Captured Plan Grammar Templates` section** — a side-by-side comparison table of the four captured cluster grammars (passing 1-4, setting 5-6, attacking 7-11, defense 12-16) showing all four share a six-slot focus-agnostic spine (opener / primer / per-shot technique / movement / read-or-pressure overlay / live-feed transfer / competitive wrapper) with focus-specific content per slot — generalizes the M001 archetype shape (`warmup` → `technique` / `movement_proxy` → `main_skill` → `pressure` → `wrap`) with source-backing and identifies slots 5-6 as where read-and-decision drills concentrate; (2) **`Cross-Source Convergence Map` section** — a 12-row table mapping where BAB / FIVB / VDM-LTD3 agree, disagree, or fill different gaps; (3) **new T13 (Repetition Signals Priority; Novelty Signals Architecture)** — meta-thesis on the BAB authoring economy with ~30 distinct source forms accounting for ~120 captured drill slots; (4) **per-thesis evidence tags (T1-T13)** — each thesis now declares its evidence base (source count and origin) and the strongest competing reading; this makes BAB-only / 1-2-case schema proposals (T11, T12) visibly distinguishable from multi-source corroborated claims (T2, T3, T5, T7); (5) **two new structural sections** — `Patterns That May Be Coaching Template, Not BAB-Specific` (acknowledges that the six-slot grammar may be standard coaching pedagogy not BAB-specific, the controlled-feed→live-overlay sequence may be FIVB Modified Games framing rebranded, and the universal warm-up may be one author's habit not pedagogical proof) and `Theses Retired or Narrowed` (an empty structural slot for falsification — the synthesis has accumulated 13 theses without retiring any, which is itself a confirmation-bias signal worth surfacing). Quick Scan now opens with **honest framing: "this is a BAB-dominant synthesis with FIVB and VDM/LTD3 as guardrails, not a balanced tri-source synthesis"**, names depth imbalance (Game Play has 0 captured plans), and notes the effective unique-drill count is much smaller than plan-count. T4 softened to align with `docs/research/vdm-development-matrix-takeaways.md` Item 1's explicit constraint that VDM stage vocabulary should not be operationalized as a runtime gate without a separate decision. Plan-Builder Theses split into "existing repo canon restated" (1-8) and "synthesis-derived" (9-10) to make it visible which claims are evidence-backed vs product-preference. Net effect: the synthesis doc is now epistemically honest about its BAB-dominance, sample-size limitations, and falsification gap; usable as a recipe for future plan-builder work *with explicit caveats about which schema decisions are licensed and which are still gated on corroborating evidence*.
- 2026-05-04: BAB Practice Plan 16 (Defensive) integrated into the BAB source archive (`docs/research/bab-source-material.md`). **Fifth and final defensive practice plan** captured at source-detail level — **completes the captured Defensive cluster (Plans 12-16)**, which now provides the most complete single-focus plan-grammar template in the BAB set so far. Partial source capture: Drills 1 and 2 (Best Warm Up Ever, Triangle Setting) are name-only — Triangle Setting is structurally implied to be the 4-player hit-fed source form from Plans 10/11/14 but not source-confirmed. Plan 16 contributes one structurally novel pair of drills and several alias-chain extensions: (1) **`Threat or No Threat` (Drills 5-6)** is the first BAB source for **blocker-as-reader read drills** — the blocker reads set quality (a teammate's contact, not opponent action) and decides between two committed actions (block if good set / pull off net to dig if bad set); structurally distinct from prior captured read drills where the attacker reads (Plans 9, 11) or the defender reads (Plan 14); first BAB drill where the cue source is **team-context** rather than opponent action; expands the read-drill schema from a single rotating "deceiver role" axis to a three-axis taxonomy (`readerRole` × `deceiverRole` × `cueSource`); also confirms the **toss-fed → serve-fed sibling pair pattern** as a recurring BAB authoring pattern (Drills 5-6 are the same drill structure with only feed type differing, structurally parallel to Plan 3's `Passing Triangle from Toss/Serve` pair); (2) **`Best of 3, Match to 21` (Drill 7)** is a new competitive wrapper format with a `third-to-15` tiebreaker rule — composable with Plan 15's `win by 2` extension, together they confirm scoring rules should be modeled as composable parameters (base score-to-N + tiebreaker-to-M + win-by-margin extension as three independent parameters); (3) Plan 16 extends multiple alias chains: Cross Court Pepper to **four plans** (Plans 5 / 7 / 11 / 16), Pull Digs to Set to two plans (Plans 14 / 16), Triangle Setting (4p hit-fed) to **five plans** (Plans 10 / 11 / 14 / 15 / 16 — name-only in 15 and 16). Practice-plan authoring synthesis updated: T10 (read/look-and-react drills) sharpened with the three-axis read-drill schema (`readerRole`, `deceiverRole`, `cueSource`); Tier 3+ candidate routing extended with Plan 16 Drills 5/6/7. The captured Defensive cluster (Plans 12-16) provides the most complete single-focus plan-grammar template in the BAB set: opener → Triangle Setting (setting touch) → defensive technique foundation → defensive movement (`4 Steps to Paradise`) → smack-misdirection / pull-block / blocker-decision drills → controlled-feed → live-point integration → competitive wrapper. Routing updated in `docs/research/README.md` and `docs/catalog.json`. No code or canon changes; all Plan 16 novel content remains Tier 3+ defense candidates.
- 2026-05-04: BAB Practice Plan 15 (Defensive) integrated into the BAB source archive (`docs/research/bab-source-material.md`). **Fourth defense-focused practice plan** captured at source-detail level (partial — Drills 1 and 2 are name-only; Plan 15 Drill 2 Triangle Setting is structurally implied to be the 4-player hit-fed source form from Plans 10/11/14 but not source-confirmed). Plan 15 is structurally a **repetition + extension** plan with two genuinely new contributions: (1) **`Shuffle to High Line/Cut Shot to Live Play`** (Drill 6) — first BAB source for the live-point integration of the Plan 14 Shuffle drills; 4-player drill where Team A digs CS (Round 1) and HL (Round 2) from Team B's tossed shots, then plays out the point live; structurally parallel to how Comp Transition HL/CS Live is the live-play extension of earlier per-shot work; confirms a recurring BAB plan-grammar pattern of pairing a controlled-feed technique drill with a live-point integration drill; (2) **`Mini Games to 7 (SHOTS ONLY)`** (Drill 7) — fourth documented rule-restriction variant of the Mini Games to 7 family (after First Ball Focused Shot / Two Players Battle from Plans 5/12, Round 1 and 2 ONLY from Plan 8); adds two constraints: SHOTS ONLY (no hard-driven attacks) and **`win by 2`** (composable scoring extension — first BAB capture that names this rule explicitly). Plan 15 also extends multiple existing alias chains: 14-15 Games wrapper to **six plans** (Plans 9 / 10 / 11 / 13 / 14 / 15); Triangle Setting (4p hit-fed) to **four plans** (Plans 10 / 11 / 14 / 15 — name-only in 15); Shuffle to HL Dig and Shuffle to CS Dig to two plans each (Plans 14 and 15); confirms `4 Steps to Paradise` source-detail across two plans (Plans 12 and 15), upgrading the cross-reference row from "Plan 15 TOC also lists" caveat to "source-detail confirmed across both plans." Practice-plan authoring synthesis updated: T9 (rule-restricted/augmented variants) sharpened with the Mini Games to 7 family as the strongest source case for the single-base-drill-with-parameter-overlays modeling approach (four documented rule-restriction variants now). The `win by 2` scoring extension argues for **composable scoring rules** (base game-to-N format + extension like win-by-margin) rather than enumerated as separate scoring types. Routing updated in `docs/research/README.md` and `docs/catalog.json`. No code or canon changes; all Plan 15 novel content remains Tier 3+ defense candidates.
- 2026-05-04: BAB Practice Plan 14 (Defensive) integrated into the BAB source archive (`docs/research/bab-source-material.md`). **Third defense-focused practice plan** captured at source-detail level (partial — Drill 1 is name-only). Plan 14 deepens the captured BAB defense plan grammar with two structurally novel drill patterns and extends two existing alias chains to five plans each. Five new findings now captured in the BAB glossary, cross-reference table, and synthesis: (1) **`Pull Digs to Set`** (Drill 3) — first BAB source for pull-block-and-transition-set source detail beyond glossary level; 4-player drill with two named scenarios (pull-line, pull-angle) and a transition-setting variant where the blocker becomes the setter on the third contact; introduces `transition setting` as named BAB vocabulary; closes the long-standing pull-dig glossary entry with full source provenance; (2) **`Shuffle to High Line Dig` / `Shuffle to Cut Shot Dig`** (Drills 4-5) — first BAB source for an **attacker-deception read drill**; the attacker fakes a hard hit by smacking the ball, then actually tosses a finesse shot, training defenders to start one direction on the smack and change direction late on the actual shot; sibling per-shot variants per Thesis T8; structurally distinct from Plan 9 Drill 3 (defender-deception) and Plan 11 Drill 5 (blocker-deception) — together forming a three-cell taxonomy of read drills with rotating deceiver position; (3) **smack-misdirection feed pattern** — distinct from feed-type primitives (`partner-toss`, `live-serve`); future Volleycraft drill metadata may need a `feedPattern` field separate from `feedType`; (4) **`deceiverRole` axis** for read drills — Plans 9, 11, and 14 form a three-cell taxonomy (defender / blocker / attacker) suggesting `deceiverRole: 'defender' | 'blocker' | 'attacker' | 'none'` as a future read-drill schema axis; (5) **two more alias-chain extensions** — Comp Transition family extends to five plans (Plans 7 / 10 / 12 / 13 / 14); 14-15 Games to 21 wrapper extends to five plans (Plans 9 / 10 / 11 / 13 / 14); Triangle Setting (4-player hit-fed) confirmed across three plans (10 / 11 / 14). The captured BAB defensive technique foundation is now a six-drill set across Plans 12–14. Practice-plan authoring synthesis updated: T10 (read/look-and-react drills) extended with the three-position deceiver taxonomy and the `feedPattern` vs `feedType` distinction. Routing updated in `docs/research/README.md` and `docs/catalog.json`. No code or canon changes; all Plan 14 novel drills remain Tier 3+ defense candidates.
- 2026-05-04: BAB Practice Plan 13 (Defensive) integrated into the BAB source archive (`docs/research/bab-source-material.md`). **Second defense-focused practice plan** captured at source-detail level (partial — Drill 1 is name-only with a parenthetical framing hint `(Dig from Knees)`). Plan 13 deepens the captured BAB defense plan grammar with two genuinely new defensive technique drills and six alias-chain extensions, and supplies the strongest BAB evidence yet for **drill-repurposing across focus**. Five new findings now captured in the BAB glossary, cross-reference table, and synthesis: (1) **`Dig from Knees`** (Drill 2) is the first BAB source for an isolated low-ball one-handed scoop-dig drill; the player marks their reach radius via a "sand angel" pre-drill ritual (lay flat, stretch arms and legs out), then digs a calibrated below-net-height toss above antenna height — sibling to Plan 12 Drill 2's lateral-extension dig, the pair forms the defensive technique foundation; (2) **`Hand Dig / Platform Dig Set - 2 sets`** (Drill 4) is the first BAB source for a multi-touch defensive sequence with hand-dig technique (overhand at chest/head height vs platform/forearms) + set return + transition release back to defense + second dig; introduces three new vocabulary items (hand dig, platform dig contrast, release back to defense) for continuous defensive engagement; (3) **defender-perspective framing of per-shot HL/CS drills** — Drills 5 and 6 (`Toss to HL`, `Toss to Cut Shot`) are word-for-word identical to Plans 8/9 per-shot HL/CS attack drills, but Plan 13 explicitly re-frames the named focus character from attacker to defender ("Player 1 reacts and catches shot"); strongest BAB evidence yet for Thesis T1 (focus is purpose, not a tag) — drill is unchanged, practice context shifts from attack-focus to defense-focus by renaming the named focus character; (4) **six alias-chain extensions in one plan** — Plan 12 Drill 2 ≡ Plan 13 Drill 3, Plan 8 Drill 2 / Plan 9 Drill 4 ≡ Plan 13 Drill 5, Plan 8 Drill 3 / Plan 9 Drill 5 ≡ Plan 13 Drill 6, Plan 7 Drill 5 ≡ Plan 13 Drill 7, the Comp Transition family extends to four plans (Plan 7 / 10 / 12 / 13), and the 14-15 Games wrapper extends similarly; the multi-name-aware cross-reference table is now load-bearing across at least eight separate aliasing observations; (5) **defensive technique foundation is now a four-drill set** — Plan 12 Drill 2 (lateral extension), Plan 13 Drill 2 (low-ball scoop), Plan 13 Drill 3 alias of Plan 12 Drill 2, and Plan 13 Drill 4 (multi-touch sequence); future high-line dig sibling structurally implied per Thesis T8. Practice-plan authoring synthesis updated: T1 sharpened with Plan 13's defender-perspective evidence as the strongest source case for drill-repurposing across focus. Routing updated in `docs/research/README.md` and `docs/catalog.json`. No code or canon changes; all Plan 13 novel drills remain Tier 3+ defense candidates.
- 2026-05-04: BAB Practice Plan 12 (Defensive) integrated into the BAB source archive (`docs/research/bab-source-material.md`). **First defense-focused practice plan** captured at source-detail level (partial — Drill 1 is name-only); earlier captures were passing (Plans 1–4), setting (Plans 5–6), and attacking (Plans 7–11). Plan 12 begins the captured Defensive cluster (Plans 12–16) and surfaces five new findings now captured in the BAB glossary, cross-reference table, and synthesis: (1) **first per-shot defensive technique drill** — Drill 2 (`Partner Toss Dig to Cut Shot`) is a 2-player drill where P2 tosses cut shots from the net and P1 uses one crossover step + laydown/dive to dig and lift to antenna height; dig-side analog of Plan 8 Drills 2/3 `Hitting a HL / CS`; sibling per-shot variant to a future `Partner Toss Dig to High Line` (not yet captured but structurally implied); (2) **full source for `4 Steps to Paradise`** — Drill 3 closes the previously name-only cross-reference entry; 3-player defensive movement drill (defender + two simulated attackers at each antenna); defender works in cross-court "Paradise" position then runs the 4-step crossover/step/plant/plant footwork to transition to the next dig spot; defense-side analog of `Footwork for Setting` (`d40` in M001); foundation rung for a future defense chain; (3) **drill-repurposing-across-focus pattern** — Drill 4 (`Serve a Spot - Dig Set Hit`) is the same physical drill as Plan 2 Drill 4 (`Serve Spot, Dig Set Hit`); BAB authors a single drill once and cites it across plans with different focus framing (Plan 2 frames it for passing emphasis, Plan 12 for defensive emphasis) rather than authoring per-focus duplicates; converges with Thesis T1 in `practice-plan-authoring-synthesis.md` (focus is purpose, not a tag); (4) **defense plan grammar** — Plan 12's structure (warm-up → per-shot dig technique → defensive movement → integrated chain → competitive wrapper) mirrors the attack and setting plan grammars at the structural level, confirming that BAB practice-plan templates are focus-agnostic at the structural level; (5) **three more drill-name aliasing cases** — Plan 12 Drill 4 ≡ Plan 2 Drill 4; Plan 12 Drill 5 ≡ Plan 7 Drill 7 ≡ Plan 10 Drill 8 (extending the existing alias chain to three plans); Plan 12 Drill 6 ≡ Plan 5 Drill 8 (`Mini Games to 7 (Two Players Battle) (Option 1)` ≡ `Mini Games to 7 (First Ball Focused Shot)` — Plan 12's name is more descriptively accurate). Practice-plan authoring synthesis updated: T1 (focus is purpose, not a tag) sharpened with the drill-repurposing-across-focus observation; new open question added on whether drill records should expose a `compatibleFocuses` axis or let session-builder selection match drills to focuses by structural properties. The cross-reference table is now multi-name aware across at least seven separate aliasing observations spanning Plans 2 / 5 / 7 / 8 / 9 / 10 / 11 / 12. Routing updated in `docs/research/README.md` and `docs/catalog.json`. No code or canon changes; all Plan 12 novel drills remain Tier 3+ defense candidates.
- 2026-05-04: BAB Practice Plan 11 (Attacking) integrated into the BAB source archive (`docs/research/bab-source-material.md`). Fifth attack-focused practice plan captured at source-detail level (partial — Drill 1 is name-only). Plan 11's most important deliverable is **the full source text for `Beat the Blocker`** (Drill 5), closing the Plan 9 Drill 6 `Beat the Blocker Cross/Line` re-solicitation caveat — the drill is now a 4-player hard-hit reactive-blocker attack drill where the blocker chooses among grab / block line / block cross and the attacker reads to challenge (swing at the block) or avoid (swing away), with two big scoring squares on Line and Angle and an explicit "okay to get blocked" learning frame. Plan 11 also introduces three other patterns now captured in the BAB glossary, cross-reference table, and synthesis: (1) **rule-augmented variants** — Plan 11 Drill 4 (`+3/-3 Highlines (CS / HL with Blocker)`) augments Plan 7 Drill 4's `+3/-3 HL/CS` source form with a blocker-touch -1 penalty; sibling but inverse intent to Plan 8's rule-restricted variants (Plan 8 narrows; Plan 11 extends); both fall under Thesis T9 which now covers both patterns symmetrically; (2) **reactive blocker as a distinct blocker behavior** — Plan 10 Drills 4–7 fixed the blocker-defender pair; Plan 11 Drill 5 makes the blocker choose in real time, motivating a `blockerBehavior: 'fixed' \| 'reactive'` axis distinct from `blockerPosition`; Thesis T11 was extended to cover both modes; (3) **another `14-15 Games to 21` best-of parameter case** — Plan 11 Drill 6 locks "Best of 5" alongside Plan 10 Drill 9's "Best of 3" and Plan 9 Drill 7's "best of 3/5/7" framing, arguing for best-of as a configurable parameter on a single wrapper rather than three separate records. Drill 9 Drill 6 cross-reference row updated to show the alias to Plan 11 Drill 5 (full source detail captured); the `+3/-3` cross-skill scoring wrapper now has three documented variants (Plan 2 pass quality, Plan 7 attack accuracy without blocker, Plan 11 attack accuracy with blocker-touch penalty). Two new open questions added: `blockerBehavior` schema axis vs `blockerPosition` extension, and whether T9 modeling should treat restriction and augmentation as the same mechanism with a sign flip or two distinct mechanisms. Routing updated in `docs/research/README.md` and `docs/catalog.json`. No code or canon changes; all Plan 11 novel drills remain Tier 3+ attack candidates.
- 2026-05-04: BAB Practice Plan 10 (Attacking) integrated into the BAB source archive (`docs/research/bab-source-material.md`). Fourth attack-focused practice plan captured at source-detail level (partial — Drill 1 is name-only). Plan 10 is the densest attack-plan capture so far for new structural patterns and surfaces five new findings now captured in the BAB glossary, cross-reference table, and synthesis: (1) **defender-role-differentiated drill family** — Drills 4-7 form a four-cell matrix where the same drill skeleton varies by attacker shot × blocker position × defender pickup zone, with an explicit **blocker-defender coupling rule** (when blocker takes line, defender takes cut shot; when blocker takes cross, defender takes highline) — the beach-defense analog of the moving-triangle passing theory; (2) **fourth Triangle Setting source form** — Drill 2 is a 4-player hit-fed Triangle Setting variant distinct from `d43` (3-player) and Plan 6 Drill 4 (4-player serve-fed), bringing the captured Triangle Setting source-form count to four and arguing for explicit naming discipline in future authoring; (3) **`Serve to Free Ball`** — Drill 3 is the first BAB source for a sideout-to-free-ball-transition drill, arguing for separating serve reception from free-ball reception in catalog metadata; (4) **level-scaled rules within a single drill description** — Plan 10 Triangle Setting specifies "Beginners catch / Intermediate-Advanced continuous" inside one drill description, distinct from Plan 2's per-level **parameter** scaling; (5) **two more drill-name aliasing cases** — Plan 10 Drill 8 ≡ Plan 7 Drill 7 (`Competitive - HL - CS - To Live Play` ≡ `Comp Transition HL - CS - Live`); Plan 10 Drill 9 ≡ Plan 9 Drill 7 (`14-15 Games to 21, Best of 3` ≡ `Games - 14-15 Games to 21`). Practice-plan authoring synthesis grew two new theses: T11 (defender position is relative to the blocker, not absolute) and T12 (some drill rules scale by level, not by parameter); two new open questions added on `blockerPosition`/`defenderPickupZone` schema axes and level-scaled-rule modeling. Cross-reference table updated to declare aliases inline (Plan 7 Drill 7 / Plan 10 Drill 8; Plan 9 Drill 7 / Plan 10 Drill 9). Routing updated in `docs/research/README.md` and `docs/catalog.json`. Also fixed a stray-backtick markdown rendering bug in two Plan 9 implementation-consequences bullets carried over from the prior pass. No code or canon changes; all Plan 10 novel drills remain Tier 3+ attack candidates.
- 2026-05-04: BAB Practice Plan 9 (Attacking) integrated into the BAB source archive (`docs/research/bab-source-material.md`). Third attack-focused practice plan captured at source-detail level (partial — Drills 1 and 6 are name-only). Plan 9 surfaces three new patterns now captured in the BAB glossary, cross-reference table, and synthesis: (1) **drill-name aliasing** — Plan 8 `Hitting a High Line / Cut Shot` and Plan 9 `Offensive Accuracy - HL / CS` are word-for-word identical except for the drill name, the first source evidence that BAB drill names are not stable across plans (the cross-reference table is now multi-name aware); (2) **vision/read attack-pepper** — `3 Touch Vision Pepper` is the first BAB source for an attack-side look-and-react drill where the attacker reads the defender's hand position and chooses where to drive (converges with FIVB Drills 3.15 / 4.9 "Look" cues); (3) **competitive game wrappers** — `14-15 Games to 21` and `Set to 21` are session-shape wrappers, not technique drills, and should not occupy the authoring-budget cap as drill records. Drill 6 (`Beat the Blocker Cross/Line`) was provided as name-only and is the candidate `blockerPresence: 'blocked'` complement to Plan 7 Drill 5's open-net swing; re-solicit before authoring. Practice-plan authoring synthesis grew T8 (sharpened with name-aliasing observation) and added T10 (read/look-and-react drills as a distinct subkind). Two new open questions added: cross-reference alias mechanism and `successMetric` shape for read-decision drills. Routing updated in `docs/research/README.md` and `docs/catalog.json`. No code or canon changes; all Plan 9 novel drills remain Tier 3+ attack candidates.
- 2026-05-04: BAB Practice Plan 8 (Attacking) integrated into the BAB source archive (`docs/research/bab-source-material.md`). Second attack-focused practice plan captured at source-detail level. All eight Plan 8 drills marked up: `Best Warm Up Ever` is the universal opener; `Hitting a High Line` and `Hitting a Cut Shot` (Drills 2 and 3) are new per-shot technique candidates that supply the foundation for Plan 7's shot-accuracy work; `Around the World (Toss)` and `Around the World (Serve)` (Drills 4 and 5) are repeated source evidence for the BAB 7-zone attack convention (now confirmed verbatim across Plans 1, 4, and 8); `High Line - Cut Shot - Live (only shots)` (Drill 6), `3 Before 5 (Serve 1 Player) (SHOTS ONLY)` (Drill 7), and `Games to 7 - Round 1 and 2 ONLY` (Drill 8) are rule-restricted variants of earlier drill families. Plan 8 introduces two new authoring patterns now captured in the BAB glossary and synthesis: **per-shot variants** (one drill record per named shot rather than a configurable parameter — observed across Plan 7 Drill 4, Plan 8 Drills 2/3, and Plan 8 Drill 8 rounds) and **rule-restricted variants** (`SHOTS ONLY`, `(only shots)`, `Round N ONLY` constraint overlays — observed across Plan 8 Drills 6, 7, and 8). Practice-plan authoring synthesis grew two new theses: T8 (per-shot drills are sibling variants, not configurable parameters) and T9 (rule-restricted variants are a first-class authoring pattern with three viable modeling options). Two new open questions added: T8/T9 modeling decision should be aligned across the catalog. Routing updated in `docs/research/README.md` and `docs/catalog.json`. No code or canon changes; all Plan 8 novel drills remain Tier 3+ attack candidates.
- 2026-05-04: BAB Practice Plan 7 (Attacking) integrated into the BAB source archive (`docs/research/bab-source-material.md`). First attack-focused practice plan captured at source-detail level — earlier captures were passing (Plans 1–4) and setting (Plans 5–6). All seven Plan 7 drills marked up: `Best Warm Up Ever`, `Triangle Setting`, and `Cross Court Pepper` are repeated source families (no new catalog action); `+3/-3 Highlines - Cut Shots / HL`, `Highlines - Cuts - Open Net Swing`, `2 Ball Side Out`, and `Comp Transition HL - CS - Live` are new Tier 3+ attack-chain candidates with no current app records. Plan 7 introduces three new attack-related concepts now captured in the BAB glossary: per-shot **attack-accuracy boxes** (4×4 marked rectangles for a single shot, plus the two-shot-per-player geometry of `2 Ball Side Out`), the **open-net swing** pattern (attack with no blocker), and **competitive transition with a rule-delta** (the 15-ft short-attack constraint). Practice-plan authoring synthesis updated: Thesis T6 now names four attack-target conventions (BAB 7-zone, FIVB 5-zone, BAB Plan 7 accuracy boxes, product-specific) instead of three; new open questions added for `blockerPresence` axis and generic vs per-skill `+3/-3` modeling. FIVB drill-book archive cross-linked from `5.6 Around the World` to BAB Plan 7's accuracy-box convention. Routing updated in `docs/research/README.md` and `docs/catalog.json`. No code or canon changes; all Plan 7 drills remain Tier 3+ attack candidates.
- 2026-05-04: practice-plan authoring synthesis landed (`docs/research/practice-plan-authoring-synthesis.md`, status: active). Cross-source distillation of BAB Practice Plans 1–6, FIVB drill-book / coaches-manual guardrails, and Volleyball Canada / VDM stage framing into seven theses (T1 focus is a purpose not a tag; T2 feed-type honesty; T3 whole-chain default with primers; T4 stage-aware variation; T5 cap attempts not just successes; T6 zone conventions are not portable across skills; T7 BAB is pair/group native, Volleycraft must stay constraint-aware), an adopt/adapt/reject ledger, near-term content-polish / `D101` / Tier 3+ candidate routing, and eight plan-builder theses for future Volleycraft session and catalog work. Routing wired into `docs/research/README.md` (Fast Path + Notes table) and `docs/catalog.json` (research_notes + research_routing). Cross-references propagated into `docs/research/bab-source-material.md`, `docs/research/fivb-source-material.md`, `docs/research/fivb-coaches-manual-crosscheck.md`, `docs/research/vdm-development-matrix-takeaways.md`, `docs/research/ltd3-development-matrix-synthesis.md`, `docs/research/beach-training-resources.md`, `docs/solutions/2026-05-04-source-backed-content-depth-activation-pattern.md`, and `docs/decisions.md` `O22` / `O24` (synthesis-aware resolution criteria). No canon promotion: `docs/vision.md` and `docs/prd-foundation.md` untouched; the synthesis informs but does not outrank decisions per the source-of-truth hierarchy.
- 2026-05-04: `d51` Outside the Heart Serving beginner tactical-zone depth family shipped (`docs/plans/2026-05-04-004-feat-d51-beginner-serving-tactical-zone-depth-plan.md`, status: complete). Sourced from FIVB Drill-book 2.2 Serving Outside the Heart, three variants (`d51-solo-open`, `d51-pair-open`, `d51-pair`), workload envelope 8-14 min / 14 min fatigue cap / 32 reps. New `shouldPreferBeginnerServingDurationFit` selection-path reroute prefers `d51` over `d31` for beginner serving main-skill blocks above 8 minutes (mirrors the d47/d48 → d49 setting and d46 → d50 advanced passing reroutes). `SESSION_ASSEMBLY_ALGORITHM_VERSION` bumped 5 → 6. Diagnostic movement: d31 cluster groups (d31-solo-open 7/14, d31-pair-open 3/6, d31-pair 1/2 pressure_remains) absorbed (absent from receipt); new d51 groups (d51-solo-open 16, d51-pair-open 8, d51-pair 1) all classify as `likely_redistribution_caused` (clean). Total routeable groups: 62 → 75. **Third validated application** of the source-backed content-depth activation pattern (`docs/solutions/2026-05-04-source-backed-content-depth-activation-pattern.md`); first at the beginner level. Also fixed a classification gap: `movement_proxy + under_authored_min` groups now route to `workload_envelope_review` instead of `unknown_unclassified`.
- 2026-05-04: `d50` Short/Deep Pass Read advanced passing depth family shipped (`docs/plans/2026-05-04-003-feat-d50-advanced-passing-depth-plan.md`, status: complete). Sourced from FIVB Drill-book 3.13 Short/Deep, two variants (`d50-pair-open`, `d50-solo-open`), workload envelope 8-14 min / 14 min fatigue cap / 28 reps. New `shouldPreferAdvancedPassingDurationFit` selection-path reroute prefers `d50` over `d46` for advanced pair-open / solo-open passing main-skill blocks above 8 minutes (mirrors the `d47/d48 → d49` advanced-setting reroute). `SESSION_ASSEMBLY_ALGORITHM_VERSION` bumped 4 → 5. Diagnostic movement: d46-pair-open and d46-solo-open `optional_slot_redistribution+over_authored_max+over_fatigue_cap` groups absorbed (absent from receipt); new d50-pair-open (8 cells) and d50-solo-open (12 cells) groups all classify as `likely_redistribution_caused` (clean). **First non-D47-derived advanced catalog add authored from the diagnostic kit's `source_backed_content_depth` lane**; canonical workflow learning captured at `docs/solutions/2026-05-04-source-backed-content-depth-activation-pattern.md`.
- 2026-05-02: generated diagnostics triage workflow slices through U8 landed on top of the 2026-05-01 report. The checked artifacts now include the surface contract / reserved-future theme sentinel, 53-entry triage registry, decision-debt compression lanes, workload-envelope guide routing, and redistribution causality receipt (21 generator-policy groups; 236 redistribution-affected cells; 151 cells where pressure disappears without redistribution). `U6` catalog impact preview remains deferred until there is a concrete catalog/cap proposal to preview. Freshness is checked by `npm run diagnostics:report:check` and `npm run diagnostics:triage:check`.
- 2026-05-01: generated-plan diagnostics landed on `feat/focus-coverage-readiness` (`docs/archive/plans/2026-05-01-001-feat-generated-plan-diagnostics-plan.md`, status: complete). The diagnostic sweep runs seeded `buildDraft()` outputs across the supported focus-readiness surface, fails closed on generation/context/trace/filter/off-focus errors, and routes duration-envelope pressure as observations. Current report: `docs/reviews/2026-05-01-generated-plan-diagnostics-report.md` covers 540 cells with 119 clean, 421 observation-only, and 0 hard-failure cells; observation counts are `under_authored_min` 257, `optional_slot_redistribution` 236, `over_authored_max` 288, and `over_fatigue_cap` 288 across 53 routeable groups. Report freshness is checked by `npm run diagnostics:report:check`.
- 2026-04-30: focus coverage readiness control plane landed on `feat/focus-coverage-readiness` (`docs/archive/plans/2026-04-30-002-feat-focus-coverage-readiness-plan.md`). Adds a generated readiness audit model for named Tune today focuses across pass/serve/set, current configurations, beginner/intermediate/advanced levels, and fixed 15/25/40-minute profiles; 40 minutes is the current long-session target, not 45. Focused generation now consumes existing onboarding skill level via `skillLevelToDrillBand()`, strict readiness swap checks are separated from runtime Swap's off-focus usability fallback, batch 1 made support slots focus-controlled while adding no-net `d31`/`d33` serving variants, batch 2 completed Serving readiness with FIVB-backed `d22` level/depth repair, and batch 3 completed advanced Passing/Setting with FIVB-backed `d07` activation plus new `d46`, `d47`, and `d48` families. Current audit is fully green: Passing, Serving, and Setting are each 45/45 verified under the practical floor, and generated drafts avoid repeated focus-controlled drill families while preserving the requested duration.
- 2026-04-30: pre-run flow simplification shipped (`docs/plans/2026-04-30-001-feat-pre-run-simplification-plan.md`, status: active during implementation). Tune today is now the mandatory pre-safety focus step for every pre-run draft path, with four chips (Recommended / Passing / Serving / Setting), shared focus resolution across initial picks / substitution / swaps, and in-transaction draft regeneration. Full Repeat and Repeat-what-you-did preserve focus as part of repeating the prior context; pain recovery strips focus. Setup drops Wind entirely and only asks Wall for Solo + No Net; Safety has no default focus echo and only notes focus when recovery overrides an explicit choice. Skill-scope policy lives as JSDoc on `SkillFocus`; no attack/scenario type reservation shipped.
- 2026-04-21: M001 Tier 1a landed in `docs/plans/2026-04-20-m001-tier1-implementation.md`.
- 2026-04-22 to 2026-04-24: partner-walkthrough polish, review closeout polish, and wake-lock/audio-priming infrastructure shipped.
- 2026-04-26: pre-D91 editorial polish shipped, and the founder pair pass session fired the Tier 1b `P2-3` trigger.
- 2026-04-27: `D133` pair rep-capture shipped. Pair capture moved to per-drill Difficulty plus optional Good/Total on `/run/check`; Transition refocused on Up Next; Review uses the per-drill aggregate; Dexie schema bumped to v5 for the first drill-grain capture boundary. Dexie later moved to v6 for optional streak metric capture.
- 2026-04-27: Tier 1b Layer A authored `d31`, `d33`, `d40`, and `d42`; `d36` waits on `O7`, and `d43` waits on `D101`.
- 2026-04-27: reconciled-list editorial polish shipped: Home skip-review confirm moved to `SkipReviewModal`, Settings gained the quiet logged-session footer, and `R7`/`R14` were demoted with rationale.
- 2026-04-27: solo-vs-pair variant sweep added tuned Pair variants and the `participants_label_mismatch` catalog validation guard.
- 2026-04-27: V0B-28 per-drill success criterion moved into the Drill Check count surface through `getBlockSuccessRule`.
- 2026-04-27: cca2 dogfeed synthesis captured eight validated findings (F1–F8) and same-day fixes for `d26` cooldown copy plus technique/movement-proxy slot picking.
- 2026-04-27: cca2 F1/F8 follow-ups shipped role/skill eyebrows, removed per-block rationale prose from Run/Transition, added courtside-copy rule 6, swept m001 candidate drill instructions, and centered Run/Transition/DrillCheck headers with grid layout.
- 2026-04-28: app architecture pass U0–U10 landed (`docs/plans/2026-04-26-app-architecture-pass.md`, status: complete). Atomic-commit cleanup, field-merging review-draft writes (`patchReviewDraft`), capture-domain consolidation with a metric-strategy registry, `services/review/` SRP split, new pure `app/src/model/` layer with `db/` demoted to adapter, controller decomposition + `app/src/platform/` for browser-runtime concerns, forward-compatibility seams (`SessionParticipant[]`, `SkillVector`, `ExportSession` adapter, `CoachPayload` type), the P12 screen contract registry + sunset rule for `runFlowInteractionContract`, refreshed `.cursor/rules/` + testing pyramid map, durable `docs/ops/app-architecture-guidance.md` for future agents, and a final holistic red-team gate (one P2 controller-import fix; two P3 follow-ups recorded). 10 atomic commits, 1065/1065 Vitest pass, typecheck + production build clean.
- 2026-04-28: per-drill-capture-coverage Phase 1 (gap 2b) and per-drill-success-criterion (V0B-28 surface-move) close-out — both plans shipped inside the U2 capture-domain consolidation (`11fed34`) and stale plan statuses synced. The Drill Check eligibility decision is now keyed off the metric-type strategy registry in `app/src/domain/capture/eligibility.ts::resolveDrillCheckCaptureEligibility`: count-eligible drills (`pass-rate-good` / `reps-successful`) capture regardless of slot type (only `warmup` / `wrap` excluded), so `d10-pair` at technique and `d03-pair` at movement_proxy stop falling silent (cca2 dogfeed F1 + F2 gap 2b closed). The V0B-28 forced-criterion prompt now renders the per-drill success rule + anti-generosity nudge inside the expanded `Add counts` body, sourced from `variant.successMetric.description` via `getBlockSuccessRule`; `PassMetricInput`'s rate label is skill-neutral `% good`; `d33`'s `successMetric.description` reads the per-attempt rule `Serve lands in the called zone.` on both Solo and Pair siblings; spec `primarySkillMetric` block reads `the drill check surface (post-D133)` with the source-from-record clause. Phase 2 (gap 2a, non-count main_skill drills get a per-`successMetric.type` capture story) stays gated on (i) Phase 1 live for ≥4 sessions, OR (ii) ≥2 founder-ledger sessions where Difficulty-only is explicitly noted as insufficient, OR (iii) partner walkthrough ≥P1 specifically on the gap. Verification: 1065/1065 Vitest, lint clean, build clean.
- 2026-04-28: per-drill-capture-coverage Phase 2A streak capture shipped as a bounded `D134` founder-use exception. `/run/check` now offers an optional collapsed `Add longest streak` drawer for `streak`-typed main-skill / pressure drills, persists `metricCapture?: { kind: 'streak'; longest: number }` inside `perDrillCaptures`, and bumps Dexie to v6. Phase 2B metric shapes remain deferred unless the pre-registered dogfood gate clears.
- 2026-04-28: build-17 founder + Seb pair dogfeed (15 min, no net, RPE 5, completed). Positive validation for the new role/skill eyebrow + drill subcategory cues and for the optional-count posture (Seb: "I like the option to count… you don't always want to, or feel forced like you need to"). Repeated frictions: cooldown / sub-block pacing beeps were missing or inaudible despite the 2026-04-24 wake-lock + audio-primer ship, and the landing-page / "option to choose a new difficulty setting" remained Seb's "consistent point." Follow-up audio investigation found two app-side gaps and applied a narrow fix: Run now holds wake lock while the active block is planned/prerolling/running, and active recovery candidate `d25-solo` now carries 30 s pacing metadata. Residual boundary remains unchanged: silent switch, manual lock, and unsupported/denied Wake Lock can still make audio unreliable. Focus-picker and skill-level mutability subsequently fired their respective triggers under `D135` (2026-04-28) — see the 2026-04-28 `D135` entry below; the optional-count posture is now a pre-registered design constraint on any future Framing C live counter (`docs/plans/2026-04-26-pair-rep-capture-tier1b.md`). Findings: `docs/research/2026-04-28-build17-pair-dogfeed-feedback.md`; follow-up: `docs/research/2026-04-28-audio-pacing-reliability-investigation.md`. Ledger: `docs/research/founder-use-ledger.md` 2026-04-28 row.
- 2026-04-28: per-move pacing indicator V4 (each-side stretches) shipped (`docs/plans/2026-04-28-per-move-pacing-indicator.md`, status: complete). Dogfeed flagged that some segments are intrinsically unilateral (hip stretch crossing one ankle, shoulder stretch one arm across chest, every d26 stretch with a "back leg" or "front leg" anatomy) but the segment timing didn't account for both sides — d26 deferred the second side to a bonus "mirror" clause, d25 just ran one-sided. Resolution: new `eachSide?: boolean` on `DrillSegment`. `durationSec` stays as total time on the timer (no math gymnastics — preserves catalog validation, scaling math, pacing math); `<SegmentList>` appends a muted " (each side)" suffix so the user knows to switch sides during the segment. d26 promotes mirror from bonus into the floor — all 3 stretches `eachSide: true` at 60s (~30s/side); bonus copy drops the mirror clause and is now purely accessory ("add glutes or adductors"). d25 hip + shoulder marked `eachSide: true` with durations bumped 30s→60s; sum raised 180s→240s; `durationMinMinutes` bumped 3→4. d25 becomes the longer-form unilateral cooldown; d26 stays the compact 3-min option (15-min wrap slots pick d26 at 3min, either at 4min). d28 warmup unchanged (no unilateral segments). The `eachSide` flag rides through `scaleSegmentsForBlockDuration` automatically, so Shorten preserves "(each side)" on rescaled segments. Verification: 1189/1189 Vitest pass (1184 → 1189, 5 new tests covering eachSide rendering + active/past/future state coverage + bilateral-only no-render guard + d26 mirror-clause-removal regression + sessionBuilder per-drill sum assertions), lint clean, typecheck clean, browser walkthrough confirmed d26 displays "(each side)" on all three rows at full duration AND after Shorten (60s → 38s), with the suffix preserved.
- 2026-04-28: per-move pacing indicator V3 (Shorten-aware) shipped (`docs/plans/2026-04-28-per-move-pacing-indicator.md`, status: complete). Dogfeed flagged that tapping `Shorten` on warmup/cooldown halved the block timer but left segment durations untouched, so the indicator ran off the end of shortened blocks (segments 3-4 silently never happened on a halved d28). Resolution: when `activeDuration < sum(segments[].durationSec)`, scale every segment proportionally via the new pure helper `scaleSegmentsForBlockDuration` in `app/src/domain/runFlow/segmentTiming.ts` — the user does ALL moves at faster timing, preserving warmup-component and cooldown-stretch coverage. Asymmetric rule: scaling only fires DOWN, not UP, so long wraps (d26 on a 4-min slot) keep authored 60 s holds and surface the bonus paragraph in overflow territory. Intro copy on d28 and d26 dropped per-segment timing claims (`"~45 s each"`, `"about 60 s each"`) so the intro stays honest regardless of shortening; the segment list is now canonical for per-move timing. d25 intro had no time claim and stays. Verification: 1184/1184 Vitest pass (1174 → 1184, 10 new tests covering scale + controller wiring + intro updates), lint clean, typecheck clean, production build clean, browser walkthrough confirmed Shorten on Beach Prep Three drops segment durations 45s → 25s and intro stays correct.
- 2026-04-28: per-move pacing indicator V1/V2 shipped (`docs/plans/2026-04-28-per-move-pacing-indicator.md`, status: complete). Closes the visible-channel half of S1 from `docs/ideation/2026-04-28-what-to-add-next-ideation.md`. New `DrillSegment` type + `DrillVariant.segments?` + `courtsideInstructionsBonus?` ride the existing `subBlockIntervalSeconds` snapshot pipeline (catalog → variant → pick → DraftBlock → SessionPlanBlock → swap → runner). All three M001-active timed drills now declare structured segments: `d28-solo` (4×45s = 180s), `d26-solo` (3×60s = 180s + bonus copy for mirror/glutes/adductors), `d25-solo` (5 segments / 60+30+30+30+30 = 180s + bonus copy for hydrate; `durationMinMinutes` bumped 2→3 to anchor honestly to the segment sum — every authored wrap slot already required min ≥ 3). Pure timing math at `app/src/domain/runFlow/segmentTiming.ts` (`computeSegmentState`); per-segment end beep via `useBlockPacingTicks` extension; `<SegmentList>` at `app/src/components/run/SegmentList.tsx` extends the existing coaching-cue sidebar voice (left rule + small filled accent dot for the active row, success check for past, hollow ring for future; bonus paragraph rendered only after all segments complete). Zero motion. **2026-04-28 in-session manual-test iteration (V1 → V2)**: V1 used a "NOW" text pill that misaligned labels across rows and a `ml-auto` duration that landed on the first line of wrapping labels; V2 (shipped) replaced the pill with a small accent dot equal-width to the other markers (CSS grid `grid-cols-[1rem_1fr_auto]` so labels align), and `self-end` on the duration cell so it sits at the end of the LAST line on multi-line wraps. `cue?` reserved-field discipline applied per the `SessionParticipant[]` precedent (documented v2 trigger + regression test pinning the no-render contract). Catalog validation rejects `segment_duration_mismatch` / `duplicate_segment_id` / `invalid_segment_duration`. AE6 — visible state independent of audio state — is load-bearing per the cue-stack invariants in `docs/research/outdoor-courtside-ui-brief.md` and pinned by an audio-failure-mocked screen integration test. Verification: 1174/1174 Vitest pass (1065 baseline + 109 new), lint clean, typecheck clean, production build clean, browser walkthrough on Solo + Wall 15 min confirmed both warmup and cooldown segment indicators advance correctly in real time. Cap impact: zero (segments are not drill records). Next: founder dogfeed on iPhone PWA (warmup with screen on; cooldown with silent switch on) to confirm the indicator survives audio failure end-to-end.
- 2026-04-28: `D135` walkthrough-equivalence policy clarification shipped. Walkthrough-equivalent evidence is **source-validity gated** (real session, real build, real observation), not script gated — partner voice memos / notes / Dexie exports paired with debrief commentary count the same as a scripted partner walkthrough. Founder content-gap reports tied to real use sessions are **first-class trigger evidence** alongside ledger rows; founder *feature wishes* are *input* to founder reasoning, not trigger evidence (the boundary preserves the adversarial-memo concern that founder-use mode has no adversary in the room). The three `D130` falsification conditions, the authoring-budget cap, the channel-mix list, and the 2026-07-20 default-against-inertia decision rule are untouched. Trigger-state consequences (recorded for routing): Tier 1c trigger (`docs/plans/2026-04-20-m001-tier1-implementation.md` §"Tier 1c trigger") clause 2 **fired** by Seb's 2026-04-27 cca2 + 2026-04-28 build-17 voice memos (two independent ≥P1 partner hits naming a missing focus toggle); skill-level-mutability sibling trigger (same plan §"Skill-level mutability — separate surface, separate trigger") partner-walkthrough OR clause **fired** by the same two memos directly naming skill-level inflexibility. Implementation of either surface is its own future ship under sequencing option A — same precedent as `D133` (trigger fired 2026-04-26 → ship landed 2026-04-27). Adversarial memo §"Recognized behavioral-evidence channels" sharpened in the same pass with the source-validity language and the content-gap-vs-feature-wish boundary; an Amendment Log entry records the policy clarification and trigger fires. Propagated across `docs/decisions.md` (`D135`), `docs/plans/2026-04-20-m001-tier1-implementation.md` (Tier 1c + skill-level-mutability), `docs/plans/2026-04-26-pair-rep-capture-tier1b.md` (Framing C re-trigger evidence-source clarification, no count change), `docs/plans/2026-04-20-m001-adversarial-memo.md` (channels + Amendment Log), `docs/milestones/m001-solo-session-loop.md`, `docs/roadmap.md`, `docs/research/founder-use-ledger.md`, and `docs/catalog.json`.

## Routing Notes

- For v0b landed status, start with `docs/plans/2026-04-16-003-rest-of-v0b-plan.md`.
- For M001 Tier 1a / Tier 1b shipped registries, use the dated plans named in `docs/catalog.json`.
- For active founder-use evidence, use `docs/research/founder-use-ledger.md`.
- For open questions and decided constraints, use `docs/decisions.md`.
