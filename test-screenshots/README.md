This folder holds browser-test and validation screenshots used during prototype review and field-test prep.

- `test-screenshots/` contains timestamped captures referenced by docs such as `docs/research/2026-04-12-v0a-runner-probe-feedback.md`.
- The root-level PNG files under `app/` are also captured prototype artifacts, not runtime image assets.
- Keep screenshots only when they support a current doc, bug report, or validation trail.
